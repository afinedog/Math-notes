# 模论

## 基本概念

- **左 $R$ 模**：
  - 设 $A$ 是加法交换群
  - 若对于环 $R$，存在映射（数乘作用） $R\times A\to A$ 满足
    - 数乘左右分配律：$r(a+b) = ra + rb$，$(r+s)a = ra + sa$
      - <font color='chartreuse'>推论：$r(na) = n(ra)$，这里 $n\in\Z$ 表示项数</font>
    - 数乘结合律：$r(sa) = (rs)a$
  - 则 $A$ 是左 $R$ 模
- **右 $R$ 模**：$A$ 和 $A\times R\to A$
- **酉/幺 $R$ 模**：$R$ 是含幺环，且 $1_Ra = a$
  - <font color='red'>一般来说极少讨论含幺环的非幺模，所以后面只要 $R$ 含幺，就默认它的模是幺模</font>
- **向量空间**：若 $R$ 是除环，则左/右 $R$ 模称为 $R$ 上的左/右向量空间
  - <font color='red'>元素均可逆的R模</font>
- **作用定理**：模就是被环作用的加法交换群
- **实例**：
  - **整数模（阿贝尔群）**：加法交换群都是幺 $\Z$ 模，作用为 $n\times a = na$
  - **子环模（子环作用）**：若 $S$ 是环，$R$ 是子环，则 $S$ 是 $R$ 模<font color='red'>（反之不成立）</font>
    - **实例**：多元多项式环、形式幂级数环都是R模
    <!-- - **多项式模**：若 $K$ 是环，$X$ 是子集，则 $K[X]$ 是 $K$ 模，作用为 $k\times \sum\limits^n_{i=1}k_ix^i = \sum\limits^n_{i=1}(kk_i)x^i$ -->
  - **理想模（理想作用）**：设 $I\in \I_L(R)$，则 $I$ 是左R模，作用为 $r\times a = ra$
    - **实例**：$(0),R$ 都是R模
  - **商环模**：设 $I\in\I(R)$，则 $R/I$ 是R模，作用为 $r\times (r_1+I) = rr_1+I$
      - 若 $I$ 是双向理想，则 $R/I$ 必须是环
  - **回撤模**：若 $\p:R\to S$ 是环同态，则S模都是R模，作用为 $r\times x = \p(r)x$
    - <font color='red'>构造同态是改变系数环的常用手法
    - 同态都可看作一种收缩，收缩后（S）的模在收缩前（R）当然也是模</font>
  - **自同态模**：交换群 $A$ 是自同态环 $\End A$ 的模，作用为 $f\times a = f(a)$
  - **平凡模（全零模）**：任意交换群，任意环，都存在平凡模，作用为 $r\times a = 0$

### 习题

- **行列定理**：
  - 右向量空间的元素可写成列向量，线性变换是矩阵左乘
  - 左向量空间的元素可写成行向量，线性变换是矩阵右乘
  - **证明**：
    - 右向量空间元素 $v = \tvec{x_1 \\ \vdots \\ x_n}$，右乘运算 $vd = \tvec{x_1d \\ \vdots \\ x_nd}$，线性变换 $f(v) = Av$
      - **线性**：$f(vd) = A(vd) = (Av)d = f(v)d$，系数在内和在外时都是右乘
      - **复合**：$f\circ g(v) = A(Bv) = (AB)v$，矩阵和映射的顺序一致
      - <font color='red'>如果换为行向量表示，则上面顺序会取反</font>

## 模同态

- **R模同态**：R模之间的映射 $f:A\to B$，满足加法公式和数乘公式<font color='chartreuse'>（线性组合公式）</font>
  - **实例**：
    - 零映射是任何模的同态
    - 阿贝尔群同态是 $\Z$ 模同态
    - 升幂映射 $X:R[x]\to R[x]，f\mapsto xf$ 是R模同态，但不是环同态
      - <font color='chartreuse'>不满足环乘法公式：$X(fg) = xfg \neq xfxg = X(f)X(g)$
      - 但满足模乘法公式：$X(rf) = xrf = rxf = rX(f)$</font>
- **线性变换**：若 $R$ 是除环，则R模同态等价于线性变换
  - **实例**：
    - **实线性变换**：$R = \R^{n\times n}，A = \R^n$，则 $r\times a = R_{n\times n}(a_1,...,a_n)^T$
- **R模范畴**：全体 $R$ 模构成范畴，态射是 $R$ 模同态

## 子模

- **子模**：设 $A$ 是模，若加法子群 $B$ 关于原作用封闭，则称为 $A$ 的子模
  - **实例**：
    - **回撤模的子模**：$\ker f$ 是定义域的子模，$\Im f$ 是陪域的子模，子模的同态逆像是子模
    - **理想模的子模**：若 $I\in \I_{LR}$，$S\subset A$，则 $IS$ 是 $A$ 的子模
  - **反例**：
    - $A = \Z$ 是 $(2)$ 模，但 $(3)$ 不是 $(2)$ 模
  - **传递性**：
    - **交传递性**：两个R模的交也是R模
    - **遗传性**：子模的子模是子模
- **子空间**：向量空间的子模

### 生成子模

- **子集生成的子模**：
  - **闭包定义**：所有包含 $X$ 的 $A$ 子模之交
  - **集合定义**：$\lang X \rang = \hkh{\sum\limits^s_{i=1} r_ia_i + \sum\limits^t_{j=1} n_jb_j \Bigm| a_i,b_j\in X，r_i\in R，n_j\in \Z}$
  - **乘积定义**：$\lang X \rang = RX + \Z x$
- **含幺生成子模**：若 $R$ 是含幺环，则 $\lang X \rang = RX$
- **循环子模**：单元素生成的模
  - **集合表示法**：$\lang a \rang = \hkh{ra+na\mid r\in R，n\in \Z}$
  - **乘积表示法**：$\lang a \rang = Ra + \Z a$
  - **实例**：
    - $\Z$ 是 $\Z$ 模，由 $\lang 1 \rang$ 生成
    - $\Q$ 是 $\Q$ 模，由 $\lang 1 \rang$ 生成<font color='chartreuse'>（$\Z$ 不是 $\Q$ 的 $\Q$ 子模，因为有理数乘运算不封闭）</font>
- **含幺循环子模**：若 $R$ 是含幺环，则 $\lang a \rang = Ra$
  - **实例**：线性代数的循环子空间
- **生成并**：$\ip{\mathop{\bigcup}\limits_{i\in I}B_i} = B_{i_1} + B_{i_2} + \cdots$
  - <font color='red'>（不保证无交，故不是直和）</font>

## 商模

- **（定理1.5）诱导同态**：设 $A$ 是R模，则 $R\to Ra，r\mapsto ra$ 是R模满同态
  <!-- - 子模的和就是元素和的全集 -->
- **（定理1.6）商模**：
  - 设 $A$ 是R模，$B$ 是 $A$ 的子模
  - 则商群 $A/B$ 是R模，作用为 $r\times (a+B) = ra+B$
- **商映射（规范同态）** $\pi:A\to A/B，a\mapsto a+B$ 是R模满同态，$\ker \pi = B$

### 同构基本定理

- **（定理1.7）第一同构定理**：
  - 设 $R$ 是环，$f:A\to B$ 是R模同态，$C$ 是 $\ker f$ 的R子模
  - 则
    - 存在唯一的 $R$ 模同态 $\ol f:A/C\to B，a+C\mapsto f(a)$
    - $\ol f$ 是R模同构 $\LR f$ 是R模满同态，且 $C = \ker f$
  - **证明**：和群相同
  - **推论**：$A/\ker f\cong \Im f$
- **（推论1.8）第二同构定理**：
  - 设 $R$ 是环，$A',B'$ 分别是两个R模 $A,B$ 的子模，$f:A\to B$ 是R模同态，$f(A')\subset B'$
  - 则
    - 存在R模同态 $\ol f:A/A'\to B/B'，a+A'\mapsto f(a)+B'$
    - $\ol f$ 是同构 $\LR \Im f + B' = B，f^{-1}(B')\subset A'$
  - **证明**：和群相同
  - **推论**：
- **（定理1.9）第三同构定理**：
  - 设 $B,C$ 是环 $B$ 上模 $A$ 的子模
  - 则
    - 存在R模同构 $B(B\cap C)\cong (B+C)/C$
    - 若 $C\subset B$，则 $B/C$ 是 $A/C$ 的子模，且存在R模同构 $(A/C)/(B/C) \cong A/B$
  - **证明**：和群相同
- **（定理1.10）子模的商传递性**：
  - 设 $R$ 是环，$B$ 是R模 $A$ 的子模，$C$ 是包含 $B$ 的子模
  - 则存在双射 $\p: \{A\}\to \{A/B\}，C\mapsto C/B$
  - **证明**：
  - **本质**：每个 $A/B$ 的子模都具有 $C/B$ 形式

## 直积

- **（定理1.11）直积**：设 $R$ 是环，$\{A_i\}$ 是非空 $R$ 模族，$\prod\limits_{i\in I} A_i$ 是群直积，$\sum\limits_{i\in I}A$ 是群直和
  - **模传递性**：$\prod\limits_{i\in I} A_i$ 是R模，$r\{a_i\} = \{ra_i\}$
  - **弱性**：$\sum\limits_{i\in I} A_i$ 是 $\prod\limits_{i\in I} A_i$ 的子模
  - **规范投影**：$\pi_k:\prod A_i\to A_i$ 是满同态
  - **规范逆投影**：$\iota_k: A_k\to \sum A_i$ 是单同态
- **（定理1.12）范畴R模直积**：
  - 设 $R$ 是环，$\{A_i\}$ 是非空 $R$ 模族，$C$ 是R模，$\p_i:C\to A_i$ 是R模同态族
  - 则存在唯一的R模同态 $\p:C\to \prod\limits_{i\in I} A_i，\pi_i\p = \p_i$
  - <font color='red'>直积由同构 $\p$ 唯一决定</font>
  - **证明**：和群相同
- **（定理1.13）范畴R模余积**：
  - 设 $R$ 是环，$\{A_i\}$ 是非空 $R$ 模族，$D$ 是R模，$\psi_i:A_i\to D$ 是R模同态族
  - 则存在唯一的R模同态 $\psi:\sum\limits_{i\in I} A_i\to D，\iota_i\psi = \psi_i$
  - <font color='red'>直积由同构 $\psi$ 唯一决定</font>
  - **证明**：和群相同
- **（定理1.14）直和判定**：
  - 设 $R$ 是环，$A$ 是R模，$\{A_i\}^n_{i=1}$ 是R模族
  - 则 $A\cong \sum\limits^n_{i=1} A_i \LR \forall i，\exists \pi_i，\iota_i$ 满足以下条件
    - 互逆性：$\pi_i\iota_i = 1_{A_i}$
    - 正交性：$\pi_i\iota_j = 0\pad (i\neq j)$
    - 组合性：$\sum \pi_i\iota_i = 1_A$
  - **证明**：由范畴R模余积定理，取规范投影和规范逆投影验证即可
  - **本质**：类似前面环的正交性习题
- **（定理1.15）弱内直积**：
  - 设 $R$ 是环，$\{A_i\}^n_{i=1}$ 是R模 $A$ 的子模族
  - 若满足
    - $A$ 是 $A_i$ 的普通和<font color='chartreuse'>（$\forall a\in A，\exists \{a_i\in A_i\}，a = \sum a_i$）</font>
    - $\forall k\in I，A_k\cap \sum\limits_{i\neq k}A_k = 0$
  - 则 $A \cong \sum\limits_{i\in I} A_i$
  - **证明**：直和判定直得

## 习题

- **极大极小定理**：
  - $A+B$ 是包含 $A,B$ 的最小子模
  - $A\cap B$ 是同时含于 $A,B$ 的最大子模
  - **证明**：反证法易得

### 映射性质

- **左右逆与消去律**：设 $f:A\to B$ 是R模同态
  - **左侧单同态可消去**：$f$ 是单同态 $\LR \forall g,h:D\to A$，若满足 $fg = fh$，则 $g = h$
    - **证明**：定义易得
    - **本质**：单态射等价于左逆唯一
      - 单射和原像唯一性关系密切，故右逆唯一
  - **右侧满同态可消去**：$f$ 是满同态 $\LR \forall k,t:B\to C$，若满足 $kf = tf$，则 $k = t$
    - **证明**：定义易得
    - **本质**：满态射等价于右逆唯一
      - 由满射性得左侧映射的原像相同，故不能取分段映射来创造不同的映射法则，从而左逆唯一
  - <font color='chartreuse'>其实和R模没什么关系，这是泛函知识，或者说范畴知识</font>

### 单模（不可约模）（循环模）

- **单模**：无平凡子模的非零模
  - <font color='red'>和群不同，模天然具有交换性，故任意子模都可作商，从而没有"正规子模"的概念</font>
  - **循环性**：单模 $\LR$ 循环模
    - **证明**：若 $A = \lang a,b,... \rang$，则由交换性，$\lang a \rang$ 是R子模，即 $A$ 不是单的
  - **同态双射性**：单模的自同态都是零映射或自同构（置换）
    - **证明**：
      - 若不是单射，由单模循环性，$f(ka) = 0$。再由同态数乘性，$f(a) = 0$，$A$ 是零模，矛盾。
      - 若不是满射，则存在 $ka\neq \forall f(ma)$，显然不成立
- **循环模理想定理**：循环幺R模同构于R的某个主理想
  - **证明**：
    - 若 $\char R = 0$，结论易得
    - 若 $\char R = n$，
- **循环模商环定理**：循环幺R模同构于R的某个左理想商环
  - **证明**：
    - 设 $R$ 含幺，$\lang a \rang$ 是幺R模，$I$ 是左理想
    - 设 $f:\lang a \rang\to R/I$，要使其为R模同构
      - <font color='chartreuse'>左边是对模进行循环约束，右边是对环进行商约束。若要使两种约束等价，只需要找到在循环模下等价的 $R$ 中元素，将这些元素取为理想（商等价类）即可。即 $r_1 a = r_2 a$，取理想 $I_b = \set{r\in R\mid ra = b}$ （水平集）即可</font>
      - 即 $f:\lang a \rang\to R/I_b，(r+n)a \mapsto (r+n+I_b)，(n\in\Z)$
      - 同态公式：任取 $r_0\in R$
        - $f(r_0(r+n)a) = f((r_0r+nr_0)a) = r_0r + nr_0 + I_b$
        - $r_0f((r+n)a) = r_0( r+n+I_b ) = r_0r + nr_0 + I_b$
      - 双射性：分别讨论 $r，n$ 易得单射性，再由 $r,n$ 任意性即得满射性。
  - **本质**：水平集商环都可以写成循环形式

### 半单模（完全可约模）

- **完全可约模**：设 $A$ 是R模，则下列三条性质等价
  - **直和分解性**：$A$ 是单模的直和
  - **补性**：对任意子模 $B$，都存在子模 $C$ 使得 $A = B\oplus C$
- **实例**：
  - 单模是完全可约模
  - 单模的直和是完全可约模
- **传递性定理**：
  - **遗传性**：完全可约模的子模也完全可约
    - **证明**：
      - 设 $A = B\oplus C$
      - 若 $B$ 是单模，则已成立
      - 若 $B$ 不是单模，则存在子模 $D$。再由完全可约性，存在 $A = D\oplus E$，显然 $B = D\oplus (B\cap E)$
  - **直和传递性**：
    - **证明**：显然

### 投影映射

- **幂等分解**：设 $f:A\to A$ 是R模同态，若其幂等（$ff = f$），则 $A = \ker f\oplus \Im f$
  - <font color='chartreuse'>不是线性空间，故没有维数公式，一步步来吧</font>
  - **证明**：易得像与核仅相交于 $0$
    - 只需证明 $\forall a\in A，\exists b\in \ker f，c\in \Im f$，使得 $a = b+c$ 即可
      - 若 $a\in \ker f$，则取 $b = a，c=0$ 即可
      - 若 $a\notin\ker f$，则可设 $f(a) = d$
        - **引理**：幂等单射只能是恒等映射
          - **证**：由幂等性，任取 $f(a) =b$，则 $f(f(a)) = f(b) = b$。再由 $a,b$ 任意性即得结论
        - 从而不妨设 $f$ 不是单射。此时只需考虑像集中存在多个原像的元素。即存在 $b$ 满足 $f(b) = d$。再由同态性，$b-a\in \ker f$，从而 $b,c$ 存在性得证
  - **本质**：这好像也是泛函题……而且这题没意思。不妨看看hx问我的另一个问题，证明（自伴算子与幂等算子的复合）是投影算子
- **像核维数定理**：
  - 设 $f:A\to B，g:B\to A$ 是R模同态
  - 若 $gf = 1_A$，则 $B = \Im f\oplus \ker g$
  - **证明**：
    - **像核不相交性**：易得若 $b\in \Im f$，则 $\exists f(a) = b$，从而 $a = g(f(a)) = g(b)$，从而不可能有 $b\in \ker g$
    - **直和表出性**：设 $b\notin \Im f$，即 $\forall a\in A，f(a)\neq b$
      - 再设 $g(b) = a$，再由于 $g(f(a)) = a$，由同态得 $b-f(a)\in \ker g$，即得直和性
  - **本质**：这种一眼线性空间的题统统用泛函思路做就行
- **直和分解定理**：设 $R$ 含幺，$A$ 是R模，则存在子模 $B,C$ 满足 $A = B\oplus C$
  - （其中 $B$ 是幺模，$RC = 0$）
  - **证明**：
    - 已知陪集映射 $h:A\to RA，a\mapsto Ra$ 是R模同态
    - 此时 $C = \ker h$，$B = \Im h$
    - 再由 $RRa = Ra$，得 $h$ 幂等，从而结论成立
  - **同态缩小性**：若 $f:A\to A_1$ 是R模同态，则 $f(B)\subset B_1，f(C)\subset C_1$
    - **证明（元素分析法）**：
      - $f(B)$ 中元素都可写成 $f(Ra) = Rf(a)\in RA_1 = B_1$，结论得证
      - $f(C)$ 中元素 $f(c)$，由于 $RC = 0$，故 $Rf(c) = f(Rc) = 0$
        - 再由于 $\forall c$，若 $Rc = 0$，则 $c\in C_1$，故 $f(c)\in C_1$，结论得证
    - **证明（集合比较法）**：
      - $f(B) = f(\Im h) \subset \Im hf =  B_1$
      - $f(C) = f(\ker h) \subset \Ker hf  = C_1$
      <!-- - $\ker f \cup (\ker h\cap \Im f)$，显然 $f(C) = \Im f$ -->
  - **单满传递性**：同态的单射和满射性可传递到 $B,C$ 限制上来
    - **证明**：


### 生成模

- **生成元系无退化性**：有限生成R模不一定是有限生成阿贝尔群
    - **反例**：$\Q$ 自身左模是循环模 $\lang 1 \rang$，但由前面群的习题，其不是有限生成阿贝尔群
    - **本质**：$A$ 作为模时和 $A$ 作为群时，两个生成元系是无关的
      - 因为模生成依赖于环，群生成依赖于自身
- **生成元系无遗传性**：有限生成R模的子模不一定是有限生成的
  - **反例**：
    - 取 $A = \set{f\in \Q[x]\mid 常数项为整数}$，其自身模是有限生成的
    - 取 $I = \set{f\in A\mid 常数项为0}$，其为理想
      - 反设其由 $\set{f_i}^r_{i=1}$ 有限生成，由定义，其等价于 $\forall f\in I，\exists \{g_i\}^r_{i=1}$，满足 $f = \sum\limits^r_{i=1} g_if_i$
      - 设 $f_i$ 的一次项系数为 $a_{i1}\in\Q$，$g_i$ 的常数项为 $n_i\in\Z$，则 $f$ 的一次项系数为 $\sum\limits^r_{i=1}n_ia_{i1}$
      - 由 $f$ 定义，其一次项系数是 $\Q$，也即等价于 $\Q$（$f_i$ 的一次项系数）作为 $\Z$（$g_i$ 的常数项）左模是有限生成的
    - 显然，由于分母数量的限制，任意有限个分数不能通过乘整数生成有理数集
  - **本质**：同上


### 直积

- 设 $M$ 是R模，$N,P$ 是子模，则 $\cfrac{N+P}{N}\cong \cfrac{P}{N\cap P}$
  - **证明**：