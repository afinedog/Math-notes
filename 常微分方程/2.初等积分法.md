# 常微分方程2：初等积分法

## 恰当方程（全微分方程）

- **ODE的对称形式**：$P(x,y)dx + Q(x,y)dy = 0$
- **ODE的非对称形式**：$\cfrac{dy}{dx} = - \cfrac{P(x,y)}{Q(x,y)}$
- **恰当方程**：如果存在一个可微函数 $\Phi(x,y)$，使得其全微分为其对称形式，则方程
  - **恰当性**：恰当方程一定存在解
  - **标准型**：可解的方程都可以变形为恰当方程
    - **实例**：
      - 若将恰当方程约去一些变量，则可用积分因子法求解
      - ？那里卡蒂方程呢
- **通积分（隐函数解）**：$\Phi(x,y) = C$ 称为恰当方程的通积分

### 恰当方程的判断和求解

- **判定定理**：
  - 设函数 $P(x,y)$ 和 $Q(x,y)$ 在区域 $R: \begin{cases} \alpha < x < \beta \\  \gamma < y < \delta \end{cases}$ 上连续
  - 若有连续的一阶偏导数 $\dfrac{\partial P}{\partial y}和\dfrac{\partial Q}{\partial x}$
  - 则 $Pdx + Qdy = 0$ 是恰当方程 $\LR \dfrac{\partial}{\partial y}P(x,y) \equiv \dfrac{\partial}{\partial x}Q(x,y)$
- **证明**：
  - **必要性**：由数分知识，偏导数连续 $\red\Rightarrow$ 混合偏导数相等
    - **理解**：微分中值定理 + 多元函数连续的自变量独立性
  - **充分性**：若是恰当方程，则通积分可以写为以下两种形式
    - $\dis\Phi(x,y) = \int^x_{x_0}P(x,y)dx + \int^y_{y_0}Q(x_0,y)dy$
    - $\dis\Phi(x,y) = \int^x_{x_0}P(x_0,y)dx + \int^y_{y_0}Q(x,y)dy$
    - <font color='chartreuse'>具体过程如下（待定函数积分法）：
      - 由于 $P$ 是 $\Phi$ 的 $x$ 偏导数，故 $\Phi = \dis\int^x_{x_0} Pdx + \psi(y)$
      - 对其关于 $y$ 求微分，再由题设相等条件，转化为 $\dpfrac{\Phi}{y} = Q|^x_{x_0} + \psi'(y)$
      - 再由于 $Q$ 是 $\Phi$ 的 $y$ 偏导数，故只能是 $\psi'(y) = Q(x_0,y)$
      - 综上即得第一个形式，同理可得第二个形式</font>
    - 对两种形式分别求全微分，两式结果即为两个偏导数，从而它们相等
    - **理解（自由度视角）**：
      - 选取合适的 $\Phi$ 时，自由度并不是老老实实地一部分归于 $P$，另一部分归于 $Q$，而是（$P$ 占据了所有自由度，而 $Q$ 只是用来消去 $P$ 的残值），或（$Q$ 占据了所有自由度，而 $P$ 只是用来消去Q的残值）
      <!-- - 其实，上面这两种形式才是真正的恰当方程。一开始设的 $P(x,y)dx + Q(x,y)dy = 0$ ，只是因为未知才作的保全之策。 -->

#### 习题

- **求解恰当方程**：
  - **直接法**：直接写出恰当方程的通解
    - 取 $(x_0,y_0) = (0,0)$ 最简单
  - **分组凑微分法**：通过观察，把形式相似的 $x$ 和 $y$ 的单项式凑到一起，有时候可以拼出乘积的微分
     - **实例**：$2x\sin ydx + x^2\cos ydy = d(x^2\sin y)$
  - 单连通区域上的单值函数（未完）

## 变量分离方程

- **变量分离法**：若方程具有形式 $X(x)Y_1(y)dx + X_1(x)Y(y)dy = 0$
  - 则其可变形为 $\dfrac{Y(y)}{Y_1(y)}dy = -\dfrac{X(x)}{X_1(x)}dx$
  - 此时可以两边直接对一元函数求不定积分
    - <font color='chartreuse'>为什么两边自变量不同，也可同时求积分？因为这里的 $y$ 实际上是 $x$ 的函数，故 $\int g(y)dy = \int g(y(x))y'(x)dx$，对 $y$ 积分就是 $x$ 的不定积分换元而已。所以实质上我们是在两边同时对两个 $x$ 的函数做不定积分</font>
- **特解**：若存在某点有 $X_1(a) = 0$，则其为原方程的解，但不能通过分离变量法得出
  - **实例**：$(x^2+1)(y^2-1)dx + xydy = 0$
    - 在 $x=0$ 时有解
    - 在 $y=0$ 时无解
- **微分方程定义（导数）理解法**：
  - *微分方程解的整体性*：如果令x=0，则解只能是x=0，它不能分割成x和y两个条件（否则就只是一个无价值的线素）
  - *实例*：上例中 $\frac{dy}{dx} = \frac{(x^2+1)(y^2-1)}{-xy}$
    - 如果令 $x=0$，则相当于默认方程的解是函数 $x=0$。只需检验斜率 $k=+\infty$ 是否符合等式即可
    - 同理，可发现 $y=0$ 时，$k \neq 0$，不符合等式，不是特解。
- **微分方程定义（微分）理解法**：
  - 变量分离方程的特解就是奇异点 $\frac{dx}{X_1(x)} = \frac{0}{0}$
- **线素连续性（几何）理解法**：
  - ODE的解在单点上表现为线素，在区域上表现为线素场（所有的积分曲线）
    - 沿积分曲线移动是固定 $C$，变动 $x$
    - 沿 $x=a$ 或 $y=b$ 移动是变动 $C$，固定 $x$ 或 $y$
    - ODE有解的条件是沿积分曲线的线素必须形成一个光滑函数，也就是线素的斜率和位置必须连续变动
  - 上例中取 $x=0$，则原方程变为 $(y^2-1)dx = 0$，为了使连续的 $y$ 都能成立等式，必须有 $dx = 0$，即 $y' = +\infty$
    - 综上线素为 $x=0$ 且 $k=+\infty$，其连续，故 $x=0$ 是ODE的解
  - 上例中取 $y=0$，则原方程变为 $(x^2+1)dx = 0$，为了使连续的 $x$ 都能成立等式，要有 $dx = 0$，即 $y' = +\infty$
    - 综上线素为 $y=0$ 且 $k=+\infty$，其不连续，故 $y=0$ 不是ODE的解
- **总结**：变量分离方程的特解一般是 $P(x,y)或Q(x,y) = 0$，即 $k=\infty或0$ 时产生。且都是和坐标轴平行的直线。

### 解的局部性质（例子）

- **解的局部唯一性**：若某点 $A$ 上存在多个积分曲线 $\Phi_1,...,\Phi_k$，则称其为局部不唯一点
  - <font color='chartreuse'>由于ODE只有一个，故这些积分曲线在 $A$ 点的斜率都相同（除了特殊曲线 $x=x_0$），造成它们不同的原因是 $A$ 以外存在某点斜率不同</font>
- **孤立的奇异点**：$\cfrac{dy}{dx} = \cfrac{(x^2+1)(y^2-1)}{-xy}$
  - 通解为 $y^2 = 1+C\dfrac{e^{-x}}{x^2}$
  - 特解为 $x=0$ 和 $y=\pm 1$
  - **无奇异点**：解在 $\R^2$ 上均存在
  - **局部不唯一点**：在 $(0,1)$ 上，有两个特解 $x=0$ 和 $y = 1$，即 $$
- **连续的奇异点**：$\cfrac{dy}{dx} = \cfrac{3\sqrt[3]{y}}{2}$
  - 通解为 $y^2 = (x+C)^3$
  - 特解为 $\begin{cases} x < C \\ y=0 \end{cases}$
  - **局部不唯一点**：该段上有无限个积分曲线的重叠，解局部不唯一）
- **瑕积分定理（局部唯一性）**：已知微分方程 $\dfrac{dy}{dx} = f(y)$，
  - 若
    - $f(y)$ 在直线 $y=a$ 的某邻域 $(a-\varepsilon,a+\varepsilon)$ 内连续
    - $f(y) = 0 \LR y=a$
  - 则
    - $y=a$ 上解局部唯一 $\LR \dis|\int^{a\pm\varepsilon}_a \frac{dy}{f(y)}| = +\infty$
  - **证明**：
    - 方程可转化为 $\dis\int^y_{y_0} \frac{dy}{f(y)} = x-x_0$
      - 左端原函数设为 $G(y) = x$，则方程为 $G(y)-G(y_0) = x-x_0$
    - 由题设易得 $y=a$ 是微分方程的解
    - **充分性（正证法）**：
      - 由瑕积分发散得 $x(a+\e)-x(a) = +\infty$，也就是说对任意在 $y=a$ 以外存在定义的积分曲线 $\Phi$，不可能存在一个 $x_0$ 使得 $\Phi$ 在 $(x_0,a)$ 上有定义
      - 也就是说，$\Phi$ 只能以 $y=a$ 为渐近线，从而解局部唯一
    <!-- - **充分性（反证法）**：
      - 由于斜率 $f(y)$ 只与 $y$ 有关，故通解的积分曲线彼此不相交，故考虑反例时只需要存在一个积分曲线 $\Phi$ 在 $y=a$ 其上和其外均有定义即可
        - 不妨反设 $\Phi(x_0,a) = A$，且在外部有定义
      - 即此时 $\Phi'_0(x_0,a) \neq 0$，从而 $\dfrac{1}{f(y)}$ 也连续
        - 由闭区间上连续函数的性质，$\dfrac{1}{f(y)}$ 在 $(a+\e,a-\e)$ 上内闭一致有界
      - 再由瑕积分不收敛，故只能是 $\dfrac{1}{f(y)}$ 在 $a\pm \e$ 处都无界，但这等价于 $$ -->
    <!-- - **必要性（正证法）**：
      - 若解局部唯一，则解只有 $y=a$
      - 从而 $\forall x\in R，G(a) = x$ 无解，即 $G(y)$ 在 $y=a$ 处无定义或极大
      - 再由 $f(y)$ 局部连续性，其可积，从而 $G(y)$ 有定义，那么只能是极大反常积分
      - <font color='chartreuse'>反常积分是否存在N-L公式？尝试举一个反例（导数趋于无穷，但函数有界）
        - $y = x^2$，则 $f(y) = \dfrac{1}{2\sqrt{y}}$，此时 $\dis\int^{y_0}_y \dfrac{1}{2\sqrt{y}} = x_0-x$，但在 $y\to 0$ 时 $\dfrac{1}{f(y)}$ 无界
        - 但是虽然函数无界，但反常积分是收敛的，也就是说N-L公式还是成立的……啊，是我糊涂了。反常积分的值不就是用N-L公式定义的吗……无论是收敛还是不收敛，都可以使用N-L公式</font> -->
    - **必要性（反证法）**：
      - 反设瑕积分收敛，则由N-L公式有限性，得存在 $x_1$ 使得 $\dis\int^{y_1}_{y_0} \frac{dy}{f(y)} = x_1-x_0$，即存在一条积分曲线在 $(x_1,a)$ 和 $(x_0,y_0)$ 上都有定义，从而解局部不唯一，矛盾
  - **理解**：
    - 首先，$y=a$ 是方程的特解
    - 然后，证明的核心是 $\dis\int^a_{a-\varepsilon} \frac{dy}{f(y)} = \int^a_{a-\varepsilon} dx$。它是 $y=a$ 到 $y=a-\varepsilon$ 这段区间内，$x$ 的积分值
      - 所以如果瑕积分发散，则任何连续积分曲线在 $y=a$ 上无定义，解只有一个$y=a$。（√）（后面会学到，这叫做解延伸到开区间边界）
      - 如果解局部唯一，反设瑕积分收敛即可
  - **本质**：
    - 右侧条件表示 $\int^{a+\varepsilon}_a dx = +\infty$，即靠近 $y=a$ 时 $x$ 趋向于无穷，也就是取不到，只能作为渐近线。
    - 如果能取到，那么切线会导致 $y=a$ 上的点的解（线素）局部不唯一
  - **实例**：
    - $\frac{dy}{dx} = \sqrt{|y|}$，直线为 $y=0$
      - 瑕积分不发散，其上均为切线
    - $\frac{dy}{dx} = \begin{cases} yln|y|, y\neq 0 \\ 0, \qquad y=0
    \end{cases}$， 直线为 $y=\pm 1$
      - $y=1$ 上瑕积分为 $ln(lny)$，发散
      - $y=-1$ 上瑕积分为 $ln(ln(-y))$，不发散
    - $y = \begin{cases}Ce^{-\int p(x)dx}，y\neq 0\\ 0，y=0\end{cases}$
      - 在 $y=0$ 的邻域内，指数函数连续且趋近于横线。瑕积分发散，$y=0$ 上的解局部唯一

## 一阶线性方程

- **一阶线性方程**：$\cfrac{dy}{dx} + p(x)y = q(x)$
- **分类**：
  - **齐次线性方程**：$q(x) = 0$
    - **解**：变量分离即得 $$y(x) = Ce^{-\int p(x)dx}$$
  - **非齐次线性方程**：$q(x) \neq 0$
    - **解（积分因子法）**：
      - 设 $\mu = e^{\int p(x)dx}$，方程两边乘 $\mu$，则左边变为全微分
      - 得解为 $$y = e^{-\int p(x)dx}(C + \int q(x)e^{\int p(x)dx}dx)$$
    - **解（分步构造法）**：
      - 尝试构造 $y$ 使其满足 $y' = -p(x)y + q(x)$
      - 关于 $p(x)y$ 项，由 $e$ 的导数不变性，解中必定存在 $e^{-\int p(x)dx}$ 项
      - 关于 $q(x)$ 项，由不定积分的线性，可用常数变易法设解为 $e^{\int p(x)dx}M(x)$，然后代入求解即可

### 线性微分方程的性质（类比高代线性方程组）

- **齐次线性方程存在平凡解**：齐次线性方程必定存在特解 $y=0$，且通解无零点
   - **证明**：$y = \begin{cases}Ce^{-\int p(x)dx} & y\neq 0\\ 0 & y=0\end{cases}$
     - 在 $y=0$ 的邻域内，指数函数连续且趋近于横线。瑕积分发散，从而 $y=0$ 上的解局部唯一
     - <font color='chartreuse'>类似齐次线性方程组必有零解</font>
- **解的整体存在性**：若区间 $I$ 上 $p(x)$ 和 $q(x)$ 有定义且连续，则 $I$ 上始终存在解（无奇异点）
  - **证明**：已知指数函数无奇异点，故由通解公式直得结论
- **（齐次方程）解的线性封闭性**：齐次线性方程 $E$ 的解的线性组合依然是 $E$ 的解
  - **证明**：由通解公式得同一个线性方程的不同解只有系数不同，故线性封闭
  - **推论**：解还形成一个函数线性空间（详见线性微分方程组）
- **（非齐次方程）解的线性组合性**：非齐次线性方程 $E'$ 的某一个解，可与其齐次形式 $E$ 的通解相加，从而表出所有 $E'$ 的解
  - **证明**： 由通解公式易得结论
- **解的存在唯一性**：柯西问题中，解函数存在且唯一
  - **证明**：由通解公式 + 上面对特解的讨论易得结论

### 习题

- **不定积分到定积分的转化**：
  - *一般形式*：$\int f(x)dx = \int^x_0 f(s)ds + F(0)$
  - *零点形式*：$\int f(x)dx = \int^x_{x_0} f(s)ds$（原函数零点）
  - 特殊例：$$\dis\int f(x)e^{\int g(x)dx}dx = \int^x_0 f(s)e^{\int^s_0 g(t)dt + G(0)} ds + FE_{(fe^{\int})}(0)$$
  - **推论**：
    - 齐次线性微分方程中，积分常数 $C = y(0)e^{P(0)}$
    - 非齐次线性微分方程中，积分常数 $C = y(0)e^{P(0)}$


- **周期函数方程**：
  - 设ODE为 $\dfrac{dy}{dx} + ay = f(x)$
  - 若 $a>0$，且 $f(x)$ 为周期 $2\pi$ 的连续函数
  - 求方程的 $2\pi$ 周期解
  - **解**：
    - 其为一阶线性ODE，故通解形式为 $y(x) = Ce^{-ax} + \int^x_0 e^{-a(x-s)}f(s)ds$
    - 问题转化为：求使 $y$ 成为周期函数的 $C$
    - **引理**：$y(0) = y(2\pi) \red\Rt y(x+2\pi) \equiv y(x)$
      - **证明**：
        - 若解满足周期性，则 $y(x+2\pi)$ 也是方程的解
          - <font color='chartreuse'>即有 $\dfrac{dy}{dx} + ay = f(x+2\pi)$
          - 相当于把y往x轴左侧移动了 $2\pi$，因为微分不变，所以 $f(x)$ 也不变，所以方程仍然成立</font>
        - 设 $u(x) = y(x+2\pi)-y(x)$，则 $u(x)$ 为相应齐次方程的解
          - 由题设条件，$y(0)=y(2\pi)$，即此时 $u(0) = 0$
          - 由线性齐次方程解的性质得 $u(x) \equiv 0$，即 $y(x+2\pi)\equiv y(x)$（**证毕**）
        - <font color='chartreuse'>本质就是线性齐次微分方程的解若有零点则恒为0</font>
    - 代入 $y(2\pi) = y(0)$ 即可求出 $C = \cfrac{\int^{2\pi}_0 e^{as}f(s)ds}{e^{2\pi a}-1}$
    - 将C代入通解即得 $2\pi$ 周期解
  - **理解**：
  - **本质**：周期函数的ODE，其周期和常数 $C$ 相关

### 习题

- **积分因子法**：设 $x\geqslant 0$，若 $y' + a(x)y \leqslant 0$，则 $y(x) \leqslant y(0)e^{-\int^x_0 a(s)ds}$
  - **证明**：左边添项 $e^{\int}$ 合并成积分后直得
  <!-- - **理解**：因为不知道y的符号，不能使用变量分离。但是使用 $e^{\int^x_0 a(s)ds}$，可以不用改变符号 -->
  - **本质**：就是线性微分方程的解法，把等号换成不等号了而已
- **对C的约束**
  - **周期函数的平均值**：
    - 齐次线性方程以 $w$ 为周期 $\LR$ $\bar{p} = \cfrac{\int^w_0 p(x)dx}{w} = 0$
    - 非齐次线性方程有唯一的周期解 $\LR$ $\bar{p} \neq 0$
    - **证明**：直接利用线性微分方程的通解，以及周期性方程 $y(0) = y(w)$ 即可得出结论
      - <font color='chartreuse'>齐次：$y(x) = Ce^{\int^x_0 p(s)ds} = y(x+w) = Ce^{\int^{x+w}_0 p(s)ds}$，即 $\dis\int^{x+w}_x p(s)ds = 0$。再由周期性即得结论
      - 非齐次：$y(0) = y(w)$ 可变形为 $C = \dis\cfrac{e^{\int}\int qe^{\int}}{e^{\int} - 1}$
        - 此时若 $\ol p = 0$，则 $e^{\int}-1 = 0$，从而 $C = \infty$，原方程无解</font>
  - **有界解**：
    - 若 $f(x)$ 在 $\R$ 上连续有界
    - 则 $y' + y = f(x)$ 仅一个有界解
    - **证明**：
      - 易得通解 $y(x) = \dfrac{C}{e^x} + \dis\int^x_0 f(s)e^{s-x}ds$
        - <font color='chartreuse'>解的不同仅依赖于 $C$ 的取值不同</font>
      - 再由积分第一中值定理，$y(x) = \dfrac{C-f(\xi)}{e^x} + f(\xi)$
        - 由于 $\dfrac{1}{e^x}$ 无界，$C$ 和 $f(\xi)$ 均有界，故只有 $C = f(\xi)$ 时才能使 $y(x)$ 有界（**证毕**）

## 初等变换法

- **初等变换法**：通过换元将方程转化为已知可解的方程<font color='chartreuse'>（变量分离方程、一阶线性方程、恰当方程等）</font>
- **实例**：
  - $\dis\frac{dy}{dx} = \frac{x^2+y^2}{2y}$
    - **解**：令 $y^2= z$ 即可
  - $\dis\frac{dy}{dx} = \frac{y}{x+y^2}$
    - **解**：调换自变量和因变量即可
  - $\dis 3xy^2\frac{dy}{dx}+y^3+x^3 = 0$
    - **解**：令 $y^3=z$ 即可
  - $\dis\frac{dy}{dx} = \frac{1}{\cos y} + x\tan y$
    - **解**：令 $z = \sin y$
  - （特殊的里卡蒂方程）$\dis\frac{dy}{dx} = y^2 + \frac{1}{x^2}$
    - **证明**：同乘 $x^2，令y = xy$，则化为变量分离方程
- **齐次方程**：$\begin{cases} P(tx,ty) = t^mP(x,y) \\ Q(tx,ty) = t^mQ(x,y) \end{cases}$
  - **解法**：令 $y = ux$，构成变量分离方程
  - **零次齐次方程**：$\dfrac{dy}{dx} = f(\dfrac{ax+by}{mx+ny})$
      - **解**：直接令 $y = ux$
  - **可化为零次齐次方程**：$\dfrac{dy}{dx} = f(\dfrac{ax+by+c}{mx+ny+l})$
      - **解**：
        - 首先求 $\begin{vmatrix} a & b \\ m & n \end{vmatrix}$
        - 若为零，则分子分母线性无关，通过 $x、y$ 平移变换，消去 $c$ 和 $l$ 即可
        - 若不为0，则分子分母线性相关，直接取因变量 $u = ax+by$
- **伯努利方程**：$\dfrac{dy}{dx} + p(x)y = q(x)y^n$
  - **解法**：
    - 积分因子为 $(1-n)y^{-n}$，新元为 $u = y^{1-n}$，则此时化为一阶线性方程
- **里卡蒂方程**：$\dfrac{dy}{dx} = p(x)y^2 + q(x)y + r(x)$
  - **已知特解 $\varphi_1(x)$，求通解**：
    - 令 $y = u + \varphi_1(x)$
    - 两边消项后，可化为伯努利方程 $\dfrac{du}{dx} = \big[ 2p(x)\varphi_1(x)+q(x) \big] u + p(x)u^2$
      - <font color='chartreuse'>消去了 $r(x)$</font>
  - **特殊的里卡蒂方程**：$\dfrac{dy}{dx} = -ay^2 + bx^m$
    - 若 $m=0$，则直接转化为变量分离方程
    - 若 $m=-2$，则此时次数相匹配，可令 $z=xy$，即转化为变量分离方程
    - 若 $m = \dfrac{4k}{2k+1}$
      - 设 $\begin{cases} x = \xi^{\frac{1}{m+1}} \\ y = \frac{b}{m+1}\frac{1}{\eta} \end{cases}$，方程变为 $\frac{d\eta}{d\xi} = -\eta^2 + \frac{b}{(m+1)^2}\xi^{\large\frac{-4k}{2k-1}}$
      - 设 $\begin{cases} \xi = \frac{1}{t} \\ \eta = t-zt^2 \end{cases}$，方程变为 $\frac{dz}{dt} = -z^2 + \frac{b}{(m+1)^2}t^{\large\frac{-4(k-1)}{2(k-1)+1}}$
      - 变换k次后，$\begin{cases} a\to \frac{1}{a^{k-1}} \\ b\to \frac{b}{(m+1)^{2k}} \\ m\to 0 \end{cases}$，可化为变量分离方程
    - **推论**：$m = \frac{-4k}{2k-1}$ 也可解
  - **高阶微分方程转化为里卡蒂方程**：$y'' + p(x)y' + q(x)y = 0$
    - **解**：令 $y = e^{\int u(x)dx}$ 即可
    - **理解**：利用e的还原性达到分离因式 $e^{\int u(x)dx}$ 的目的

### 习题

- 把 $y'' + p(x)y' + q(x)y = 0$ 转化为里卡蒂方程
  - **解**：令 $y = e^{\int u(x)dx}$，再消去e幂项即可
- **一些初等变换方法总结**：
  - $y' = f(x,y)$ 是齐次函数：不用说
  - $y' = f(x+y)$：
    - **解**：
      - 设 $z = x+y$，则此时 $\dfrac{dz}{dx} = 1+f(z)$，变量分离方程
  <!-- - $y' = f(xy)$：设 $z = xy$
    - **解**：
      - 设 $z = xy$，则此时 $\dfrac{dz}{dx} = \dfrac{z}{x} + xf(z)$ -->

## 积分因子法

- **积分因子法**：在方程两边同乘一个函数 $\mu$（积分因子），使其变为恰当方程
- **单变量积分因子存在条件**：以 $x$ 为自变量的积分因子 $\mu(x)$ 为例
  - **解**：
    <!-- - 方程变为：$\mu(x)P(x,y)dx + \mu(x)Q(x,y)dy = 0$ -->
    - 乘 $\mu$ 后若为恰当方程，则：$\large\frac{\partial (\mu P)}{\partial y} = \frac{\partial (\mu Q)}{\partial x} \red\Rightarrow \frac{1}{\mu}\frac{d\mu}{dx} = \frac{1}{Q}(\frac{\partial P}{\partial y} - \frac{\partial Q}{\partial x})$
      - <font color='chartreuse'>因为左式仅以 $x$ 为自变量，所以右式也必须仅以 $x$ 为自变量
      - 没有其它的约束条件</font>
    - 此时上面的等式是一阶齐次线性方程，故可解得 $\mu = Ce^{\large\int \frac{1}{Q}(\frac{\partial P}{\partial y} - \frac{\partial Q}{\partial x})dx}$
- **积分因子变换定理**：
  - **分组法**：
    - 若方程可线性分离为 $(P_1dx + Q_1dy) + (P_2dx + Q_2dy) = 0$
    - 则对两括号内分别求积分因子 $\mu 1,\mu 2$，其公因式 $\mu$ 就是原方程的积分因子
    - **证明**：
      - **必要性**：显然不同积分因子的公因式依然是积分因子
      - **充分性**：可由引理（添项后，积分因子只小不大）得到
  - **变换法**：
    - 若 $\mu P(x,y)dx + \mu Q(x,y)dy = d\Phi(x,y)$ 成立
    - 则 $\mu(x,y)g\Big(\Phi(x,y)\Big)$ 也是方程的一个积分因子
      - <font color='chartreuse'>$\Phi(x,y)$ 在这里是隐函数，即 $\Phi(x,y) = C$ 的左式，并不是说它是常数</font>
    - **证明**：
      - 将 $\dfrac{\partial (\mu Pg)}{\partial y} = \dfrac{\partial (\mu Qg)}{\partial x}$ 展开
        - P式：$\dfrac{\partial (\mu Pg)}{\partial y} = \dfrac{\partial \mu}{\partial y}Pg + \dfrac{\partial P}{\partial y}\mu g + \dfrac{\partial g}{\partial y}\mu P$
        - Q式同理
      - 将 $P$ 式和 $Q$ 式作差：
        - 由于 $g(\Phi(x,y))$ 是复合函数，故上面P式、Q式最后一项展开后都为 $\cfrac{\partial g}{\partial \varPhi}\mu PQ$，从而可约去
        - 前两项的 $g$ 因子可约去
        - 最后转化为普通积分因子法的判别式，在题设中它已经成立。**证毕**
    - **理解**：几何意义暂时未知，代数意义就是全微分可约去，从而满足积分因子判别法
- **综合应用**：
  - **线性分离法**：求出 $\mu_1，\mu_2$，构造 $g_1，g_2$ 让 $\mu_1 g_1(\Phi_1) = \mu_2 g_2(\Phi_2)$

### 习题

- **齐次积分因子**：齐次方程中，$\mu = \dfrac{1}{xP + yQ}$ 是一个积分因子
  - **证明**：换元 $y = ux$，计算即可
  - **理解**：
    - 由齐次性，P和Q可化为仅依赖于u的函数，此时只需要对系数分离
    - 求变换后的系数，发现可添加 $(P+uQ)$ 作为分母
      - 其仅依赖于u，可约去dx的u系数部分，且不干扰du系数，从而得到变量分离方程
      - 其反变换即为积分因子
  - **常用例**：$(ydx-xdy)-(x^2+y^2)dy = 0$，积分因子为 $\mu = \frac{1}{x^2+y^2}$
- **积分因子存在判别法**：存在积分因子 $\mu = \mu(\Phi(x,y)) \LR \cfrac{\frac{\partial P}{\partial y} - \frac{\partial Q}{\partial x}}{Q\frac{\partial \Phi}{\partial x} - P\frac{\partial \Phi}{\partial y}} = f(\Phi)$<font color='chartreuse'>（即左式仅与 $\Phi$ 有关）</font>
  - **证明**：
    - 利用恰当方程的混合偏导数相等即可 $$\pfrac{\Phi}{x\pa y} = \pfrac{P}{y}\mu(\Phi) + P\frac{d\mu}{d\Phi}\pfrac{\Phi}{y} = \pfrac{Q}{x}\mu(\Phi) + Q\frac{d\mu}{d\Phi}\pfrac{\Phi}{x}$$
    - 上式可变形为 $\cfrac{d\mu}{d\Phi}\mu(\Phi) = f(\Phi)$
    - 解得积分因子为 $\mu(\Phi) = Ce^{\int f(\Phi)d\Phi}$
  - **推论**：可求出下列积分因子存在的充要条件
    - $\mu = \mu(x\pm y)$
      - **解**：此时 $\Phi = x+y$，则 $\dfrac{P_y-Q_x}{Q-P}$ 仅依赖于 $x+y$
    - $\mu = \mu(x^2+y^2)$
      - **解**：此时 $\Phi = x^2+y^2$，则 $\dfrac{P_y-Q_x}{xQ-yP}$ 仅依赖于 $x^2+y^2$
    - $\mu = \mu(xy)$
      - **解**：此时 $\Phi = xy$，则 $\dfrac{P_y-Q_x}{yQ-xP}$ 仅依赖于 $xy$
    - $\mu = \mu(\dfrac{y}{x})$
      - **解**：此时 $\Phi = \dfrac{y}{x}$，则 $\dfrac{P_y-Q_x}{yQ+xP}$ 仅依赖于 $\dfrac{y}{x}$
    - $\mu = \mu(x^\a y^\b)$
      - **解**：此时 $\Phi = x^\a y^\b$，则 $\dfrac{P_y-Q_x}{\a yQ-\b xP}$ 仅依赖于 $x^\a y^\b$
- **通积分与积分因子**：设 $P,Q$ 连续可微
  - 若 $\mu_1,\mu_2$ 是原方程的两个线性无关的连续可微积分因子，则 $\dfrac{\mu_1}{\mu_2} = C$ 是方程的通积分
  - **证明**：
    - 设 $\mu_1$ 是方程的积分因子，令 $\mu_2 = \mu_1·g(\Phi)$，则由变换法，$\mu_1$ 也是方程的积分因子
    - 再令 $g(\Phi) = \Phi$，这时 $\mu_1$ 是由 $\mu_2$ 唯一确定的。则 $\frac{\mu_1}{\mu_2} = \Phi$ 是通积分

#### 解方程

- **方程的简化处理**：
  - **先去分母并不影响结果**：$(3x+\dfrac{6}{y})dx + (\dfrac{x^2}{y} + \dfrac{3y}{x})dy = 0$
      - **解**：
        - 方程两边同时乘 $xy$ 得 $(3x^2y + 6x)dx + (x^3+3y^2)dy = 0$
        - 此时容易发现是恰当方程
- **特殊的恰当方程**
  - $(xdy + ydx) = \dfrac{1}{2}d(x^2+y^2)$
  - $\cfrac{xdy - ydx}{x^2 + y^2} = \cfrac{\frac{dy}{x} - \frac{ydx}{x^2}}{1+(\frac{y}{x})^2} = d(\arctan\frac{y}{x})$
    - 同时有积分因子 $\mu = \dfrac{1}{y^2}$，$\dfrac{1}{x^2}$，$\dfrac{1}{x^2+y^2}$
  - **应用**：一些方程可用积分因子化为上述特殊的恰当方程
    - $ydx - (x^2+y^2+x)dy = 0$
      - **解**：同时乘 $\mu_1 = \dfrac{1}{x^2+y^2}$ 得 $\cfrac{xdy - ydx}{x^2 + y^2} - dy = 0$
        - 此时左式等于 $d(\arctan\dfrac{y}{x} - y)$，可化为恰当方程
- **瞪眼法**：
  - 如果积分因子是 $(\dfrac{1}{x})^p，(\dfrac{1}{xy})^p，(\dfrac{1}{xy})^p$ 的话，从方程形式比较容易看出，可以自己猜
- **一题多解**：
  - $ydx + (y-x)dy = 0$
    - 方法一：直接求积分因子
    - 方法二：求左右两端积分因子
    - 方法三：改写为齐次方程
    - 方法四：颠倒后变为线性微分方程  
  - $y(1+xy)dx-xdy = 0$
    - 方法一：直接求积分因子 $\mu = \dfrac{1}{y^2}$
    - 方法二：换元 $u = xy$
- **分组法与变换法结合**：$y^3dx + 2(x^2-xy^2)dy = 0$
  - **解（线性分离法）**：
    - $\dpfrac{P}{y} = 3y^2，\dpfrac{Q}{x} = 4x-2y^2$
    - <font color='chartreuse'>虽然直接求是没有积分因子的，但我们容易发现可用分组法求积分因子</font>
    - 化为 $\Big[ y^3dx -2xy^2dy \Big] + 2x^2dy = 0$
      - 前式的积分因子为 $\mu_1 = \dfrac{1}{y^5}$，后式的积分因子为 $\mu_2 = \dfrac{1}{x^2}$
      - <font color='chartreuse'>由于两个积分因子没有公因，故我们还需要对其进一步变形</font>
    - 求出前式的通积分为 $\Phi_1 = \dfrac{x}{y^2} = C$，后式的通积分为 $\Phi_2 = 2y = C$
      - 由变换法结论，$\mu_1g_1(\Phi_1)$ 也是前式的积分因子，$\mu_2g_2(\Phi_2)$ 也是后式的积分因子
      - 我们想要 $\mu_1g_1(\Phi_1) = \mu_2g_2(\Phi_2)$，这样两个积分因子就存在公因
      - 寻找合适的 $g_1$ 和 $g_2$ 即可。易得 $g_1(m) = \dfrac{1}{m^2}，g_2(m) = \dfrac{2}{m}$ 符合题意
    - 故积分因子为 $\mu = \dfrac{1}{x^2y}$
  - **解（瞪眼法）**：
    - 我们猜测，积分因子一定是 $x^py^q$ 的形式，接下来只需观察方程，然后判断 $p,q$ 的值即可
  - **解（二级结论法）**
    - 由上面习题给出的判别方法得出结论


## 应用举例

### 等角轨线族（隐函数求导）（2.4的课后题）

- **通积分的隐函数性**：已知ODE的解均可写成 $\Phi(x,y) = C$
  - 若给定 $x$ 和 $y$，则可知道具体的曲线常数C。因此，$C$ 可看作是 $x，y$ 的函数
  - 所以，若给定了初值，则相当于给定了积分曲线的 $C$ 值，从而初值问题的解是一条曲线，而非曲线族
    - <font color='chartreuse'>同时，初值的选取对方程的还会有其它影响。详见解对初值的依赖性</font>
<!-- - **积分曲线族**：每个微分方程确定一个曲线族。
  - $Pdx + Qdy = 0$ 确定定义域上每点的切线
  - 积分后（隐函数形式）为 
  - 沿着函数移动，定C变x。沿着坐标轴移动，定x变C -->
<!-- - **几何曲线族**：每个等式确定一个曲线，等式的集合确定一个曲线族
  - $\Phi(x,y,C) = 0$，其中C就是可变常数，表示同族曲线的不同位置 -->
- **等角轨线族**：两个曲线族，若它们的任一曲线的交点，切线都成定角 $\a$，则称这两个曲线组互为 $\a$ 等角轨线族
- **求等角轨线族**：已知曲线族 $\Phi(x,y,C) = 0$，求其 $\alpha$ 等角轨线族
  - **解**：
    - 由于同族曲线的切线走向相同，故首先写出整体的微分方程 $Pdx + Qdy = 0$
    - **可消去C的情况**：
      - 若函数具有周期性，则可以根据 $y(0) = y(\omega)$，以及y的解来求出C。**微分方程只有一个周期解**
      - 等角曲线族，此时C成为 $C(x,y)$。C可以表示初值，可以表示
    - **不可消去C的情况**：
      - 将原方程变形，由隐函数求导定理，可得二阶隐函数的导数 $$\frac{dy}{dx} = H(x,y)  = -\cfrac{\Phi_x(x,y,C(x,y))}{\Phi_y(x,y,C(x,y))} = \cfrac{P(x,y)}{Q(x,y)}$$
        - <font color='chartreuse'>上式是原轨线族方程，不要和下面的等价轨线族方程中的 $\dfrac{dy}{dx}$ 弄混
        - 上式并不代入 $C(x,y)$（一旦代入就是0）</font>
    - 设切线角为 $\t$，则等角轨线族满足方程 $\begin{cases} \dfrac{dy}{dx} = \tan(\theta + \alpha) & 等角性 \\\\ H(x,y) = \tan\theta & 原轨线族斜率 \\\\ H(x,y) = \dfrac{P(x,y)}{Q(x,y)} & 上面的结论\end{cases}$
      - 对其求解可得 $\varPsi(x,y) = K$，即等角轨线族为 $\varPsi(x,y,K)$

### 解题思路

- 判断一阶线性方程或伯努利方程
- 判断齐次方程
- 判断恰当方程
- 判断初等因子
- 判断初等变换法