# 常微分方程6：线性微分方程组

- **符号约定**：
  - 下面所有函数 $y(x)$ 都是 $R\to R^n$ 的向量值函数，存在 $n$ 个标量函数 $f_i(x)$ 分量
  - 所有的向量都是为列向量
- **线性微分方程组**：$f_i$ 均为线性函数的微分方程组
  - **光滑性**：线性函数是光滑函数

## 解的性质

- **存在和唯一性**
  - **证明**：已知一阶线性方程满足皮卡定理，且易得皮卡定理对于向量值函数同样适用
- **线性微分方程组的标准形式**：$$\begin{cases} \dfrac{dy_1}{dx} = a_{11}(x)y_1 + a_{12}(x)y_2 + ... +  f_1(x) \\\\ \dfrac{dy_2}{dx} = a_{21}(x)y_1 + a_{22}(x)y_2 + ... +  f_2(x) \\\\ \dots \\\\ \dfrac{dy_n}{dx} = a_{n1}(x)y_1 + a_{n2}(x)y_2 + ... +  f_n(x)\end{cases}$$
- **解的标准形式**：$y(x) = \Big( y_1(x),y_2(x),...,y_n(x) \Big)^T$

### 齐次线性方程组

- **齐次线性微分方程组**：$\cfrac{\bold{dy}}{dx} = \bold{A}(x)\bold{y}$
- **解函数构成线性空间**：解集是线性函数空间 $\mathcal{S}$，维数是微分方程组的阶数
  - <font color='chartreuse'>关于函数空间的讨论详见泛函分析。在这里我们先引入一个抽象思维：任何线性空间的性质都是相似的</font>
  - **证明**：
    - **初值决定性**：若给定初值条件 $x_0\in J$ 和 $\forall y_i(x_0)$，解就由曲线族退化为曲线
    - **函数映射**：设 $H: \mathbb{R^n}\to \mathcal{S}，\bd y_0\mapsto y(x)$ 表示将初值变为相应积分曲线的映射
      - **双射性**：解的存在性 $\Rightarrow$ 满射，解的唯一性 $\Rightarrow$ 单射。因此它是双射
      - **线性**：由一阶线性方程的性质
      - **同构性**：由双射性 + 线性即得 $H$ 是同构映射
        - 即（初值向量空间 $\R^n$）和（解函数空间 $\mc S$）是同构空间，从而维数相同
    - **初值点映射**：设 $H^{-1}: \mathcal{S} \to \mathbb{R^n}$ 表示将（积分曲线）变为（相应初值点）的映射
      - <font color='chartreuse'>初值点同时也可以看作（积分曲线）和（初值向量空间中的超平面） $\Sigma_{x_0}: \{(x,\bold{y})\mid x=x_0\}$ 的交点</font>
      - **同构性**：由同构的可逆性得它也是同构映射
- **线性无关判别式**：
  - $\R^n$ 是数作为分量，数作为系数
  - $\mathcal{S}$ 是一维函数作为分量，数作为系数
    - 函数若线性相关，则：
      - 每个单项式都相同
      - 相对应的系数向量线性相关
      - 比如 $y_1 = 2e^x + 2x^2 + 1$ 和 $y_2 = 4e^x + 4x^2 + 2$ 二维相关
- **解组**：由解函数向量组成的集合（可能线性相关）
- **基本解组（基础解系）（n阶函数矩阵）**：$n$ 个线性无关向量构成的解组
  - **存在性**：齐次线性微分方程组的解空间中一定存在 $n$ 个线性无关的解
    - **理解**：
      - 一个初值对应一个解函数，而n维空间上可找出n个线性无关的初值向量
      - 因为函数不能融合，所以它和齐次线性方程组不一样
  - **通解性**：齐次线性微分方程的基本解组等价于通解
- **解矩阵 $\bold{Y}(x) = (y_{ij}(x))_{n×n}$**：解组向量排列程的矩阵
  - **基解矩阵 $\Phi(x)$**：解组是基本解组的解矩阵
    - <font color='chartreuse'>高代中的</font>
  - **齐次线性方程组的通解**： $\bd y = \bold{\Phi}(x)\bold{c}$
  - **基解矩阵的矩阵乘积封闭性**：设 $C$ 是 $n$ 阶非退化常数矩阵，则 $\psi(x) = \varPhi(x)C$ 也是基解矩阵，且 $C$ 和 $\psi(x)$ 一一对应
    - **证明**：由基变换的可逆性即可证明
    - **本质**：矩阵乘积的本质是线性变换，线性变换的本质是线性组合，而解空间对线性组合封闭
- **朗斯基行列式 $W(x) = |y_{ij}(x)|_{n×n}$**：基解矩阵的行列式
  - **刘维尔公式**：$$W(x) = W(x_0)e^{\dis\int^x_{x_0}tr[\bold{A}(x)]dx}$$
    - **证明**：
      - 对 $W(x)$ 求导，由行列式求导法则，可将其展开为逐行求导行列式的和
      - 再由原ODE的定义，可将求导行再次展开
      - 消去存在两行相等的行列式
        - <font color='chartreuse'>发现只有和原行列式的项相同（原来的行），且不存在对偶顺序（一正一反）的项才能保留（对角元），因此重新形成朗斯基行列式，得到 $\sum\limits^n_{i=1}a_{ii}W = tr[\bold{A}(x)]W$</font>
      - 得到一阶齐次线性微分方程 $W'(x) = tr[\bd A(x)]W(x)$，求解即得刘维尔公式
    - **理解**：常微分方程本来就有一定的微分自返性。而线性微分方程组的封闭性（迭代性），和行列式的归零性混合，得到了非常好的性质
    - **推论（平凡解）**：解组线性无关 $\LR$ $W(x) \neq 0$ $\LR$ $\exist W(x_0)\neq 0$
      - **几何理解**：
        - 只要是非平凡解，根据解空间 $n$ 维性，解矩阵就必定非退化
        - 根据某个解 $W(x_0)$ 就可知道是否为平凡解
      - **代数理解**：
        - 一阶线性微分方程的解因为是 $e^{-\int p(x)dx}$ ，要么 $y\equiv 0$（特解） 要么 $y \not\equiv 0$
        - 而线性微分方程组的解因为是 $W(x_0)e^{\int^x_{x_0}{trA(x)}dx}$，同样受了指数函数的影响

### 非齐次线性方程组

- **非齐次线性方程组**：$\cfrac{d\bd{y}}{dx} = \bd A(x)\bd y + \bs f(x)$
- **解的线性组合性**：$通解\bs\varphi(x) = 基解矩阵\Phi(x)\bd c + 特解\bs\varphi^*(x)$
  - **证明**：直接代入即可
- **求解方法（常数变易法）**：
  - 由向量空间的封闭性，故可设特解为 $\bs\varphi^*(x) = \Phi(x)\bd c(x)$
  - 根据原ODE + $\Phi(x)$ 的定义，$$\frac{d\bs\varphi^*(x)}{dx} = [\Phi(x)\bd c(x)]' = \bd A(x)\Phi(x)\bd c(x) + \Phi(x)\bd c'(x)$$
  - 再由于 $\bs \p^*$ 是原ODE的解，故 $$\frac{d\bs\p^*(x)}{dx} = \bd A(x)\Big[ \Phi(x)\bd c(x) \Big] + \bs f(x)$$
  - 两式作差即得 $\bs c'(x) = \Phi^{-1}(x)\bs f(x)$，对其取积分即可
  - **特解** $$\bs\p^*(x) = \Phi(x)\int^x_{x_0} \Phi^{-1}(s)\bs f(s)ds$$
  - **通解** $$y = \Phi(x)\big(\bd c + \int^x_{x_0}\Phi^{-1}(s)\bs f(s)ds \big)$$
    - **初值关系**：$\bd c = \Phi^{-1}(x_0)\bd y_0$
- **推论**：求特解必须先知道齐次形式的基解矩阵<font color='chartreuse'>（这点也和向量空间的方程组一样）</font>

### 习题

- **求解齐次线性微分方程组**
  - 直得（同一个式子中只有两个未知数）
  - 现有 $y_i$ 线性组合成新的未知数（$dy_1 = y_3$ 这种简单的线性组合）
  - 发现特解
- **证明向量值函数线性无关**
  - 若它们可以构成一个非退化线性微分方程组的解，则朗斯基行列式不（恒）为0，则线性无关
  - 若朗斯基行列式恒为0，则不能构成一个非退化的线性微分方程组的解
- **基解矩阵唯一性**：基解矩阵相同，方程组必相同
  - **证明**：基解矩阵可逆性，对称性
- **$f(x)$ 不恒为0 $\Leftrightarrow$ 非齐次线性微分方程组有且最多有 $(n+1)$ 个线性无关解**
  - **证明**：

## 常系数线性微分方程组

- **常系数线性微分方程组**：系数矩阵 $\bold{A}(x)$ 为 $n$ 阶常数矩阵，且 $\bs f(x)$ 连续
- **矩阵指数函数**：矩阵空间 $\mathcal{M}$，模定义为 $\|A\| = \sum\limits^n_{i,j=1} |a_{ij}|$ （正定性、齐次性、三角不等式）（1-范数）
  - **范数完备性（详见[矩阵分析](../数值计算/数值代数/0.矩阵分析.md)）**
  - **Cauchy不等式（范数相容性）**：$\|AB\| \leqslant \|A\| · \|B\|$
  - **幂不等式**：$\|A^k\| \leqslant \|A\|^k$
  - **零次幂**：$A^0 = E$
- **矩阵的指数函数**：$e^A = \sum\limits^\infty_{k=0}\frac{A^k}{k!}$
  - **绝对收敛性**
    - **证明**：
      - 由于 $\|A\| = m$ 是有限数，相当于证明级数 $\sum\limits^\infty_{k=1}\frac{|m|^k}{k!}$ 收敛
      - 由数分知识（Stirling公式）得 $n!\sim n^n$，故等价于证明 $\sum\limits^\infty_{n=1} (\dfrac{|m|}{n})^n$ 收敛。由Cauchy判别法即得结论
  - **非退化性**
    - **证明**：由C-H定理，矩阵 $A^k$ 均可表示为 $I_n,A,...,A^{n-1}$ 的线性组合，故有限项加和满足非退化性
      - 无限项呢？
  - **可逆性**：对任意非退化的 $A$，都有 $(e^{A})^{-1} = e^{-A}$
    - **证明**：直接验证即可
  - **保相似性**：$e^{P^{-1}AP} = P^{-1}e^AP$
    - **证明**：
      - 由相似的多项式传递性直得结论
  - **交换律条件**：级数可交换性 $e^Ae^B = e^Be^A \LR$ 乘积线性：$e^{A+B} = e^Ae^B \LR$ 矩阵可交换性 $AB = BA$
    - **证明**：
      - $(2)\LR (3)$ 在数分的级数部分已用柯西乘积证明过
      - $(1)\LR (3)$ 由定义直得    

### 基解矩阵

- **标准基解矩阵**：满足 $\Phi(0) = I_n$ 的基解矩阵
  - <font color='chartreuse'>类似自然基，是形式最简单的基础解系</font>
- **标准指数解定理**：$\Phi(x) = e^{xA}$ 是常系数齐次线性微分方程组的标准基解矩阵
  - **证明**：
    - 由矩阵指数函数的绝对收敛性，易得 $\Phi(x)$ 在有限区间上一致收敛到某个矩阵函数 $\bd A(x)$
    - 由泰勒公式的求导性质 + 一致收敛级数的逐项求导法则，可得 $\dfrac{d\Phi(x)}{dx} = A\Phi(x)$，从而 $\Phi(x)$ 是常系数齐次线性方程组的一个解矩阵
    - 再由其线性无关性，即得其为基解矩阵
  - **理解**：齐次线性微分方程具有一定程度的自返性，和 $e$ 的性质很像。这点在前面一元线性方程也已经发现了，所以可以大胆猜测多元情况的通解也具有 $e$ 的形式
- **常系数齐次线性微分方程组的通解**：$\bold{y} = e^{xA}\bold{c}$
- **常系数非齐次线性微分方程组的通解**：$\bold{y} =  e^{xA}\bold{c} + \int^x_{x_0} e^{(x-s)A}\bs f(s)ds$

### Jordan简化法

- **几种简单的矩阵指数**：
  - **标量矩阵**：$e^{\l I_n} = e^\l$
  - **对角矩阵**：$e^{\diag(a_i)} = \diag(e^{a_i})$
  - **上升矩阵**：$e^{N_n} = \sum\limits^{n-1}_{k=0} \dfrac{N^k}{k!} %= \begin{pmatrix} 1 & x & \frac{x^2}{2!} & ... & \dfrac{x^k}{k!} \\\\ & 1 & x & ... & \dfrac{x^{k-1}}{(k-1)!} \\\\ && 1 & \cdots & \vdots \\\\ & & & \ddots  \\ &&&& 1 \end{pmatrix}$
    - **上三角性**
    - **循环性**
  - **Jordan块**：设 $J_i = \lambda_i I_n + N_n$，则 $e^{J_i} = e^{\l_i}e^{N_n}$
    <!-- - 可交换性 $e^{x(\lambda E + N)} = e^{x\lambda E}e^{xN}$
    - $e^{xN} = E + xN + ... + \frac{x^k}{k!}N^k =$
    - $e^{x\lambda E} = \lambda e^xE$
    - 可求解（√） -->
  - **Jordan型**：$e^{xJ} = \diag(e^{xJ_i})$
<!-- - **Jordan对角化**：$$e^{xJ_i} = e^{\lambda_i x} \big[ I_n + xN + \frac{x^2}{2!}N^2 + ... + \frac{x^{n_i-1}}{(n_i-1)!}N^{n_i-1} \big]$$
  - **Jordan过渡矩阵**：$e^{xA} = e^{P^{-1}JP} = P^{-1}e^{xJ}P$ -->
- **Jordan简化法**：设 $\dfrac{d\bd y}{dx} = A\bd y$，若 $A = PJP^{-1}$，则对于 $\bd z = P\bd y$ 有 $\dfrac{d\bd z}{dx} = J\bd z$
  - **化简**：
    - 首先求 $A$ 的J标准型 $J$ 以及过渡矩阵 $P$
    - 再取 $\bd z = P\bd y$，即可将常微分方程组转化为容易求解的形式
  - **求解**：
    - 由于Jordan型矩阵的形式简单，故直接观察求解即可
    - 或用Jordan型基解矩阵的通项公式也可
    - **过渡基解矩阵**：由基解矩阵的乘法封闭性，$Pe^{xJ}$ 也是基解矩阵（$e^{xA}P$）

### 待定指数函数法

#### 可对角化的情况

- 若 $A$ 只有单特征根（特征子空间均为一维），则此时可对角化
  - **过渡基解矩阵**：$\varPhi(x) = e^{xA}P = P·\diag(e^{\lambda_i x})$
  - **标准基解矩阵**：$e^{xA} = \varPhi(x)\varPhi^{-1}(0) = \diag(e^{\lambda_i x})$
    - **证明**：
      - 第一式：由过渡基解矩阵的定义，$\varPhi(0) = P$
      - 第二式：由对角矩阵可交换性，消去两端的相似矩阵
  - **通解**：$\varPhi(x)\bold{c} = \begin{pmatrix} c_{11}e^{\lambda_1 x} & c_{12}e^{\lambda_1 x} & \cdots & c_{1n}e^{\lambda_1 x} \\ c_{21}e^{\lambda_2 x} & c_{22}e^{\lambda_2 x} & \cdots & c_{2n}e^{\lambda_2 x} \\ \vdots \\ c_{n1}e^{\lambda_n x} & c_{n2}e^{\lambda_n x} & \cdots & c_{nn}e^{\lambda_n x}  \end{pmatrix}_{n\times n}$
    - **证明**：由标准解矩阵 + c求得
  - **方程组的解**：$\{y_i = e^{\lambda_i x}r_i\}$
    - **证明**：
      - 设 $P = (r_1,..,r_n)$，其中 $r_i$ 是常数向量
      - 由矩阵乘积的列向量表达，$\varPhi(x) = diag(e^{\lambda_i x}r_i)$
      - （此时 $r_i$ 即为通解中的 $c_i$ ）
  - **特征向量**：$\lambda_i$ 的特征向量是 $r_i$
    - **证明**：由 $e^x$ 的导数性质，代入原方程组 $\frac{d\varPhi(x)}{dx} = A(x)\varPhi(x)$，得到 $\lambda e^{\lambda x}r = Ae^{\lambda x}r \Rightarrow (A-\lambda E)r = 0$
- 若 $A$ 存在多个特征根，但是可对角化
  - **过渡基解矩阵**：$\varPhi(x) = (e^{\lambda_1 x}r_1,...,e^{\lambda_n x}r_n)$
  - **标准基解矩阵**：某些对角元相同
  - **通解**：某些行的e相同
  - **特征向量**：某些向量属于同一个特征子空间
- 若 $A$ 存在复数特征根，但是可对角化
  - 解矩阵的形式相同，存在复数
    - **理解**：
      - 已知复数可作为实数代数方程的解，比如单位根
      - 所以微分方程同理，虽然系数都是实数，但可以有复数的解
  - **实数化定理**：实系数方程若存在解，则必定存在实数解
    - **本质**：和复相似均可化为实相似一样，两端都是实数，不可能只用复数才可以打通关系，所以必定可以用复数的某种线性组合化为实数。
  - **标准化法**：A是实数矩阵，从而由 $e^{xA} = \varPhi(x)\varPhi^{-1}(0)$，可将（复过渡基解矩阵）化为（实标准基解矩阵）
  - **线性组合法**：共轭复数解 $y_1(x)，y_2(x)$，可构造 $\begin{cases} y_{R1}(x) = \frac{1}{2}(y_1+y_2) \\ y_{R2}(x) =  \frac{1}{2}(y_1-y_2) \end{cases}$ 将其实数化
    - **共轭引理**：解函数的共轭也是解
      - **证明**：由方程计算结果是实数，得所有虚数反号并无影响

#### 不可对角化的情况

- 若 $A$ 不可对角化（存在重特征根）
  - 若 $r_0$ 是方程 $(A-\lambda_i E)^{n_i}r = 0$ 的非零解，则解 $\{r_i\} = \{(A-\lambda_i E)^ir_0\}$
  - **证明**：
    - $y = e^{\lambda_i x}(r_0 + ... + \frac{x^{n_i-1}}{(n_i-1)!}r_{n_i-1})$
    - 代入方程组，由 $f(x)·e^ax$ 的导数性质，得到 $(A-\lambda_i E)(r_0+...) = r_1+...$
    - 比较系数得到 $(A-\lambda_i E)r_j = r_{j+1}$（**Jordan型迭代公式**）
  - **推论**：解是根子空间的基

## 高阶线性微分方程


### 方程组形式

- **方程组标准形式**：设高阶线性微分方程为 $y^{(n)}(x) = \sum\limits^n_{k=1} a_k(x)y^{(k)}(x)$，则将其转化为标准方程组后
  - 系数矩阵为 $A = \begin{pmatrix} \bold{0\downarrow} & I_{n-1} \\ -a_n(x) & -\overrightarrow{\alpha}(x) \end{pmatrix}$
  - 右端向量为 $\bs f(x) = \begin{pmatrix} 0 \\ 0 \\ \vdots \\ f(x) \end{pmatrix}$
  - 显然，这是有理标准型的形式
  - **标量函数组的朗斯基行列式**：$W(x) = \begin{vmatrix} \varphi_1(x) & ... & \varphi_n(x) \\ \p_1'(x) & ... & \p'_n(x) \\ \vdots &  &  \end{vmatrix}$
    - **逐行迭代性**：高阶线性微分方程转化的方程组中，每一行都是上面一行的导数，所以它仅依赖于第一行（但不是线性关系而是导数关系）
- **刘维尔公式**：$$W(x) = W(x_0)e^{-\dis\int^x_{x_0}a_1(s)ds}\quad (a<x<b)$$
  - **证明**：
    - 易得系数矩阵中 $tr[A(x)] = -a_1(x)$
    - 故由刘维尔公式即得结论
    - <font color='chartreuse'>一阶线性微分方程的解其实也是这个形式</font>

### 二阶线性微分方程

- 此时刘维尔公式形式比较简单，但高阶就难了
- **二阶齐次线性微分方程**：$y'' + p(x)y' + q(x)y = 0$
  - **解**：
    - 由刘维尔公式得 $\varphi y' - \varphi'y = Ce^{-\int p(x)dx}$
    - 易得左式可转化为导数除法的形式，得到 $\dis\frac{d}{dx}(\frac{y}{\varphi}) = \frac{C}{\varphi^2}e^{-\int p(x)dx}$，对其求解即可
    - **通解**：$$y = \varphi(x)\Big[ C_1 + C_2\int^x_{x_0}\frac{e^{-\int^{s}_{x_0}p(t)dt}}{\varphi^2(s)}ds \Big]$$
- **二阶非齐次线性微分方程**：$y'' + p(x)y' + q(x)y = f(x)$
  - **解**：
    - 设 $W_k(x)$ 是 $W(x)$ 中序号为 $(n,k)$ 元素的代数余子式
    - 则特解为 $$\varphi^*(x) = \sum\limits^n_{k=1} \varphi_k(x) · \int^x_{x_0}\frac{W_k(s)}{W(s)}f(s)ds$$
  - **证明（整体解法）**：常数变易法，设齐次方程组的基解矩阵为 $\varPhi(x)$
    - 根据前面的结论，只需要证明 $\Phi(x)\int^x_{x_0}\Phi^{-1}(s)\bs f(s)ds \Rightarrow \sum\limits^n_{k=1} \varphi_k(x) · \int^x_{x_0}\frac{W_k(s)}{W(s)}f(s)ds$ 即可
    - 将逆矩阵转化为伴随矩阵形式，则易得 $$左式 = \int \frac{\varPhi(x)}{W(s)}W^*(s)\begin{pmatrix} 0 \\ \vdots \\ f(s) \end{pmatrix}ds = \int\frac{f(s)}{W(s)}\varPhi(x)\begin{pmatrix} W_1(s) \\ \vdots \\ W_n(s) \end{pmatrix}ds$$
    - 上式结果是列向量，其第一个分量就是右式，且**第一个分量才是解，其它的都是微分**
  - **证明（分步解法）**：
    - 首先根据齐次形式的解，设解为 $y = \sum C_k(x)\varphi_k(x)$
    - 然后求导，根据第二行的方程约去一些量，得到类似第二行方程，但含有 $C_k(x)$ 的新方程
    - 然后不断求导和约去，最后求导到最后一行方程，得到 $f(x) = ...$
    - 这样就得到了一个含有 $C_k^{(j)}(x)$ 的新的方程组，联立可以解出 $C_k^{(j)}(x)$，最后积分即可

### 常系数高阶微分方程

- **常系数高阶微分方程**：$y^{(n)}(x) = \sum\limits^n_{k=0} a_ky^{(k)}(x)$
- **基本解组**：设 $A$ 有 $s$ 个特征值，且相应重数为 $n_s$，则方程的基本解组为 $$\begin{cases} e^{\lambda_1x} & xe^{\lambda_1x} & ... & x^{n_1-1}e^{\lambda_1x} \\ e^{\lambda_2x} & xe^{\lambda_2x} & ... & x^{n_2-1}e^{\lambda_2x} \\ ... \\ e^{\lambda_s x} & xe^{\lambda_s x} & ... & x^{n_s-1}e^{\lambda_s x}\end{cases}$$
  - <font color='chartreuse'>本质就是找有理标准型的初等因子</font>
  - **证明**：
    - 由 $A$ 矩阵的形式可知，任何一个 $n-1$ 阶子式的公因子为 $1$，从而其Smith标准型中 $d_{n-1} = 1$
    - 从而 $A$ 的初等因子组无公因
      - <font color='chartreuse'>最小多项式就是特征多项式
      - 每个特征值只有一个根子空间</font>
      - 从而 $A$ 的每个特征值只有一个Jordan块
    - 设 $P$ 的元素为 $p_{ij}$
      - 由 $AP = P\diag(J_i)$ 得 $\forall p_{1m_j} \neq 0$
      - <font color='chartreuse'>每个Jordan块第一行对应的P不为0，否则退化</font>
    - 然后根据 $e^{xA}P = Pe^{xJ_i}$，得到 $e^{xA}P$ 的第一行元素：$$\begin{cases} \Big( p_{1m_1}e^{\l_1x} \Big) & \Big( *e^{\l_1x} + p_{1m_1}xe^{\l_1x} \Big) & ... & \Big( *e^{\l_1x}+\cdots+\dfrac{p_{1m_1}x^{n_1-1}}{(n_1-1)}e^{\l_1x} \Big) \\ ... \\ \Big( p_{1m_s}e^{\l_sx} \Big) & \Big( *e^{\l_sx} + p_{1m_s}xe^{\l_sx} \Big) & ... & \Big( *e^{\l_sx}+\cdots+\dfrac{p_{1m_s}x^{n_s-1}}{(n_s-1)}e^{\l_sx} \Big) \end{cases}$$
      - 因为它们都是基解矩阵的第一行元素，利用解的线性封闭性，可用初等列变换消去带 * 的项，并把其余的项系数改为 $1$，即得到题设基本解组。（**证毕**）

### 习题

- **待定系数法解非齐次方程**
  - $f(x) = P_m(x)e^{\mu x}（P_m(x)是m次多项式）$
  - 因为解的形式固定，所以可以预测解为 $\varphi^*(x) = Q_m(x)e^{\mu x}$，然后代入方程求待定系数即可
  - 如果 $\mu$ 是k重特征根，则 $\varphi^*(x) = x^kQ_m(x)e^{\mu x}$
  - 如果 $f(x)$ 是三角函数，则化为指数形式，然后取虚部即可（？）
- **化低阶为高阶**：相互代入即可（一维递进）