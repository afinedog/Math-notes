# Git在项目中的一些应用

## 远程仓库覆盖本地仓库，但是不覆盖提交记录

- 首先，使用 ```git reset --hard 远程仓库版本/pull的分支名称```，消除暂存区和工作区的所有未commit文件，回退到远程仓库版本
- 此时，git未跟踪的文件（新建的、还未暂存的文件）不受影响，需要用 ```git clean -f``` 消除这些文件


### 实例

- ```Cmodified```（分支名，本地仓库）：C++文件被我删减后的版本
- ```C++删减前的版本```（标签名）：未删减的版本，存储在远程仓库的分支上

## 