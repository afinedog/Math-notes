# Fourier展开

- 在写这一章时，一开始其实我是无从下笔的，或者说我应该是被陈纪修给坑了。它是先讲特例再推广到整体，但这一过程的质变界限是模糊的。也就是说我把对称周期函数、周期函数、非周期函数的定义和性质都搞混了，所以想改也很难改。真不如直接像实变函数泛函分析一样，直接在抽象空间中给出定义，然后推广到特殊空间和特殊函数
- 在以后的内容中，我们把抽象空间中的级数称为傅立叶级数，而它在欧氏空间中的形式称为三角级数，当然这两者本质是一样的


## 周期为 $2\pi$ 的函数（的展开式）

- **三角级数**：形如 $f(x) = \dfrac{a_0}{2} + \sum\limits^\infty_{n=1}(a_n\cos nx + b_n\sin nx)$ 的函数项级数
  - <font color='chartreuse'>当然，$f(x)$ 存在的前提是级数收敛，即 $f$ 的定义域就是级数的收敛域。这个在函数项级数章节就说明白了
  - 写成 $\dfrac{a_0}{2}$ 的原因是为了使 $a_n$ 有统一的通项公式</font>
  - **周期条件**：右侧的三角级数以 $2\pi$ 为最小正周期，故 $f$ 必须同理
    - <font color='chartreuse'>也就是说，只有周期为 $2\pi$ 的函数才能展开成这种形式。其它函数必须经过变换才行</font>
  - **可积条件**：由三角函数可积性易得 $f$ 必须满足可积性
    - <font color='chartreuse'>只有在 $\R$ 上任意有限区间可积的函数才能展开成三角级数，否则不收敛</font>
- **Euler-Fourier公式**：傅立叶级数的系数计算公式为 $$a_n = \frac{1}{\pi}\int^\pi_{-\pi} f(x)\cos nxdx \whhh b_n = \frac{1}{\pi}\int^\pi_{-\pi} f(x)\sin nxdx$$
  - **证明**：
    - <font color='chartreuse'>利用三角函数积分的正交性</font>
    - 由可积性，在展开式两端同时乘 $\cos mx$，再在两端取积分后变形易得 $\dis\int^\pi_{-\pi} f(x)\cos mxdx = a_m\pi$
    - 同理可求 $b_m$

### 特殊情况（奇偶函数）

- **奇函数**：若 $f(x)$ 为奇函数，则其展开式为**正弦级数** $\sum\limits^\infty_{n=1} b_n\sin nx$
  - **证明**：
    - 此时 $a_n$ 为奇函数的定积分，值为 $0$
    - 而 $b_n$ 为偶函数的定积分
- **偶函数**：若 $f(x)$ 为偶函数，则其展开式为**余弦级数** $\dfrac{a_0}{2} + \sum\limits^\infty_{n=1} a_n\cos nx$
  - **证明**：
    - 此时 $a_n$ 为偶函数的定积分
    - 而 $b_n$ 为奇函数的定积分，值为 $0$


## 任意周期函数（的展开式）

<!-- ### 周期是 $2\pi$ 因子的函数

- **周期延拓**：只有在 $[-\pi,\pi]$ 上定义的函数才可以直接展开。
- 我们默认R上的函数进行了周期延拓，也就是其它区间上的结果和 $[-\pi,\pi]$ 上相同 -->

- **三角级数**：设 $f(x)$ 以 $2T$ 为周期，则 $$f(x) = \frac{a_0}{2} + \sum\limits^\infty_{n=1}(a_n\cos\frac{n\pi}{T}x + b_n\sin\frac{n\pi}{T}x)$$
  - **证明（周期变换法）**：
    - 令 $t = \dfrac{\pi}{T}x$，则换元后的函数 $f(x) = \p(t)$ 的周期变为 $2\pi$
    - 按E-F公式求 $\p(t)$ 的三角系数，再换元回来即可
- **系数公式**：$$a_n = \green{\frac{1}{\pi}\int^{\pi}_{-\pi}\varphi(t)\cos ntdt} = \frac{1}{T}\int^T_{-T}f(x)\cos\frac{n\pi}{T}xdx \\ \ \\ b_n = \green{\frac{1}{\pi}\int^\pi_{-\pi}\varphi(t)\sin ntdt} = \frac{1}{T}\int^T_{-T}f(x)\sin\frac{n\pi}{T}xdx$$


## 非周期函数（的展开式）

- 此时需要Fourier变换，详见后面章节