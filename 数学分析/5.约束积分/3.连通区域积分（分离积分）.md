# 连通区域积分（求导/退化 + 分离积分）

<!-- - **反转用法**
  - 之前学习的是正用重积分的累次性，把重积分化为路径积分
  - 本节是逆用重积分的累次性，把路径积分化为重积分（因为区域的连通性，路径可以连通成边界）
- **关系**：$导数的重积分 \overset{累次公式}{\Longrightarrow} 累次积分 \overset{线性拆分、N-L公式}{\Longrightarrow} 原函数的分量积分$
  - 第一类积分不过是二维区域上的一元积分罢了（相当于把一元积分的区间拉成一条路径）
  - 第二类积分也不过是把权值积分分解为多个不相干的方向罢了
  - 两个积分的最好解决方法都是换元成一元积分（即前两章的内容）
- **总结**
  - **重积分的核心**，是利用累次积分，把重积分化为一元积分
    - 把 $y$ 向积分线的长度用 $x$ 来表示
  - **前面两章的核心**，是利用约束，把约束积分化为一元积分（不管是参数形式还是隐函数形式，都是一元形式）（弧长/面积 因子的作用是把路径拉直/铺平）
    - 把权值（向量权值）和弧长都用 $x$ 或 $t$ 来表示
  - **本章的核心**
    - *求导退化*（逆用N-L公式），约束积分退化为导数重积分，进而利用累次积分化为一元积分
    - *分离积分*，把二重积分分为线性的两部分（设为$P_x$和$Q_y$），一个积分x，一个积分y，把 x向积分线/y向积分线 的长度分别看作y/x方向的权值，就得到边界积分
- **应用**
  - 如果求导后形式简单，边界积分化为二重积分
  - 如果积分后形式简单，二重积分化为边界积分（相比于乘积，还是线性好积） -->
- 利用微分向量形式，可以很简洁地证明本章三个公式。详见[偏微分方程](../../偏微分方程/0.衔接.md)

## 曲线积分 $\leftrightarrow$ 二重积分

- **简单闭曲线（Jordan曲线）**：两端点值相等，且自身不相交的曲线
- **单连通区域**：D的简单闭曲线可以不经过D外的点，连续地收缩为单点
  - <font color='chartreuse'>实际上是拓扑同胚于单点集的区域。区域的 $k$ 连通性在复变函数中还要用，但直到代数拓扑中才能彻底讲明白</font>
- **多连通区域**：
- **诱导定向**：若点沿 $\pa D$ 移动时，$D$ 始终在左边，则称该方向为正方向。该定向方式称为诱导定向
  - <font color='chartreuse'>外部边界的正方向是逆时针，内部边界的正方向是顺时针</font>
- **标准区域 $D$**：可用两函数表示 $\pa D$ 的区域 $\LR$ 任意（平行于 $x$ 轴和 $y$ 轴的直线）最多与 $\pa D$ 交于两点的区域
  - <font color='chartreuse'>（可以是凹区域）</font>
  - **形式**：
    - $\{(x,y)\mid y_1(x)\leq y \leq y_2(x)，a\leq x\leq b\}$
    - $\{(x,y)\mid x_1(y)\leq x \leq x_2(y)，c\leq y\leq d\}$
- **Green公式（二重积分的N-L公式）**：
  - 设 $D$ 是（分段光滑的简单闭曲线）围成的单连通区域
  - 若函数 $P,Q$ 在 $D$ 上有连续偏导数
  - 则 $\dis\int\limits_{\partial D}Pdx+Qdy = \iint\limits_{D} (\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y})dxdy$（取诱导定向）
  - **本质**：
    - 实际上就是二维的N-L公式
    - 若取 $y=[0,1]$，则退化为一维的N-L公式。而由于区域的连通性，x轴、y轴积分最终和边界最终合为一点
    - 在xy轴（一维）上应用N-L公式，然后把一维相加（边界曲线积分）就是二维的N-L公式
  - <font color='red'>以 $D$ 为圆形区域为例：
    - 此时诱导定向为逆时针
    - 设左上、右上、左下、右下边界分别为 $D_1,D_2,D_3,D_4$
    - 对 $Q_x$ 关于 $dx$ 应用N-L公式
      - 结果为 $\Big[ Q|_{D_4(y)}-Q|_{D_3(y)} \Big]$ 和 $\Big[ Q|_{D_2(y)}-Q|_{D_1(y)} \Big]$（都是关于 $y$ 的一元函数）
    - 对 $P_y$ 关于 $dy$ 应用N-L公式时
      - 结果为 $\Big[ P|_{D_2(x)}-P|_{D_4(x)} \Big]$ 和 $\Big[ P|_{D_1(x)}-P|_{D_3(x)} \Big]$（都是关于 $x$ 的一元函数）
    - 对 $P$ 关于 $dy$ 边界积分
      - 在 $D$ 的左半边界，顺着正方向时 $y$ 减小，故符号为负
      - 在 $D$ 的右半边界，顺着正方向时 $y$ 增大，故符号为正
      - 即 $\dis\int_{\pa D}Pdx = -\int_{D_2}Pdx + \int_{D_4}Pdx - \int_{D_1}Pdx + \int_{D_3}Pdx$
    - 对 $Q$ 关于 $dx$ 边界积分
      - 在 $D$ 的上半边界，顺着正方向时 $x$ 减小，故符号为负
      - 在 $D$ 的下半边界，顺着正方向时 $x$ 增大，故符号为正
      - 且 $\dis\int_{\pa D}Qdy = \int_{D_4}Qdy - \int_{D_3}Qdy + \int_{D_2}Qdy - \int_{D_1}Qdy$
    - $Q_x$ 的N-L公式结果和积分结果符号相同，$P_y$ 则相反，故右式中 $Q_x$ 符号为正，$P_y$ 符号为负
    - 第二类积分具有投影性，即设 $L_x$ 是 $L:y=f(x)$ 在 $x$ 轴上的投影，$L_x$ 两端点为 $b>a$，则 $\dis\int_{L} P(x,y)dx = \int^b_a P(x,f(x))dx$。故格林公式左右两边相等
  - 二连通区域情况</font>
  - **证明**：
    - **标准区域**：右式进行累次积分，分离dx、dy，在偏导数上使用Newton-Leibniz公式 $$\iint\limits_{D}\frac{\partial P}{\partial y}dxdy = -(\int^b_aP(x,y_1(x))dx + \int^a_b P(x,y_2(x))dx) \\ = -\int\limits_{\partial D}P(x,y)dx$$。Q同理，最后两式相加即可
    - **有限块标准区域**：积分区域可加性直得结论
    - **一般情形**：参考古尔萨证明，多边形割线。这里应该算是用极小的标准区域逼近
    - **有限复连通区域（割为单连通区域，类似复周线）**：$$(\int\limits_L+\int\limits_l)Pdx+Qdx = \int\limits_{\partial D}Pdx+Qdy$$
- **几何方程形式**：$$\iint\limits_{D} (\frac{\partial F}{\partial x} + \frac{\partial G}{\partial y})dxdy = \int\limits_{\partial D} \Big[F\cos(\vec{n},x)+G\cos(\vec{n},y)\Big] ds$$

### 习题（计算积分）

- **应用**：一般都是用格林公式将曲线积分化为二重积分的，但是也有特例
  - **用格林公式求区域面积**：$S = \int\limits_{\partial D}xdy = -\int\limits_{\partial D}ydy = \frac{1}{2}\int\limits_{\partial D}xdx-ydy$
    - <font color='chartreuse'>一般都是需要应用边界方程的等式，从而实现换元化简的区域。比如星形线、双扭线等</font>
- **类留数法**：设 $P,Q$ 连续可偏导，若对任意上半圆弧都有 $\dis\int_L Pdx+Qdy = 0$，则 $P = Q_x \equiv 0$
  - **证明**：
    - 将 $L$ 补成上半圆周，使用格林公式，再分别使用积分中值定理，约分后即得 $0 = \dfrac{\pi r}{2}(Q_x-P_y)|_{P_0} + 2P(\xi,y_0)$
    - 取 $r\to 0+$ 即得 $P(x_0,y_0) = 0$。再由圆心的任意性得 $P\equiv 0$，再由上式即得 $P = Q_x$
- **复周线法**：设 $a,b > 0$，$L$ 是分段光滑闭曲线。讨论 $I = \dis\int_L \frac{xdy-ydx}{ax^2+by^2}$
  - **解**：
    - 被积函数仅存在一个瑕点 $(0,0)$
    - 易得 $P_y = Q_x$，故任意不包围原点的 $L$，都有 $I = 0$
    - 若 $L$ 包围原点，由复周线法，在 $L$ 围成的区域内，任取包含原点的周线 $L'$，都有 $\dis\int_L Pdx+Qdy = \int_{L'} Pdx+Qdy$
      - 为了化简积分，取 $L':ax^2 + by^2 = r^2$，则 $I = \dis\frac{1}{r^2}\int_{L'} xdy-ydx$，使用格林公式计算得 $I = \dfrac{\pi r^2}{\sqrt{ab}}$
- **综合题**：设 $L$ 是不过原点的简单闭曲线，求 $$I = \int_L \frac{e^y}{x^2+y^2}\Big[ (x\sin x + y\cos x)dx + (y\sin x-x\cos x)dy \Big]$$
  - **解**：
    - 被积函数仅存在一个瑕点 $(0,0)$
    - 易得 $P_x = Q_y$，故任意不包围原点的 $L$，都有 $I = 0$
    - 若 $L$ 包围原点，由复周线法，取 $L':x^2 + y^2 = r^2$，则 $I = \dis\frac{-2}{r^2}\iint_{D'} e^y\cos xdxdy$
      - 定积分不好算，故采取类留数法，取 $r\to 0+$，则由被积函数连续性 + 积分第一中值定理，即得 $I = \lim\limits_{r\to 0+} \dfrac{-2}{r^2} (e^\eta\cos\xi)(\pi r^2) = \lim\limits_{(\eta,\xi)\to (0,0)} -2\pi(e^\eta\cos\xi) = -2\pi$
  
### 习题（无用结论）

- 设 $f(x,y)$ 在圆周 $D:(0,0)，r\leq a$ 内部连续可偏导，若 $f|_{\pa D} = 0$，则 $$\biggm| \iint_{D} f(x,y)dxdy \biggm| \leq \frac{\pi}{3}a^2\max\limits_D (f_x^2+f_y^2)$$
  - **证明**：
    - 对 $(xf)_x$ 使用格林公式，得 $左式 = \dis\frac{1}{2}\iint_D (xf_x+yf_y)dxdy$
    - 再对其使用C-S不等式（内积范数），得 $\leq \dis\frac{1}{2}\iint_D \sqrt{f_x+f_y}\sqrt{x^2+y^2}dxdy$
    - 再用积分上界不等式提取出 $f$ 部分即可

### 习题（方向导数）

- <font color='chartreuse'>算是学习偏微分方程的必备预习内容？但它却并没有出现在数分教材中，而是考研书中。实际上我觉得应该把它归类为偏微分第一章的课后习题</font>
- 设 $L$ 是平面上封闭光滑曲线，$n$ 是 $L$ 的外法向量，$l$ 是任意方向向量，则 $\dis\int_L \cos(l,n)ds = 0$
  - **证明**：
    - <font color='chartreuse'> 因为 $L$ 是封闭曲线，故任意 $\vec n(x_1,y_2)$，都必定存在 $(x_2,y_2)\in L$ 使得 $\vec n(x_1,y_1) = -\vec n(x_2,y_2)$。所有的相反向量两两配对，最终积分结果就是0（当然这只是口头说明，实际必须得用坐标来做，哪怕是n维欧氏空间）</font>
    - 设 $n = \Big( \cos(\vec n,\vec x)，\cos (\vec n,\vec y) \Big)，l = (x_0,y_0)$
      - 则 $\cos(l,n) = \dfrac{1}{\sqrt{x_0^2+y_0^2}}\Big[ x_0\cos (\vec n,\vec x) + y_0\cos (\vec n,\vec y) \Big]$
      - 则 $I = \dis\frac{1}{\sqrt{x_0^2+y_0^2}}\int_L x_0dx - y_0dy$，格林公式计算得为0
- <font color='red'>诸如此类的题，一般都是把方向导数替换为 $x_i$ 的函数。在此过程中会产生方向余弦，其和 $ds$ 合并即转化为第二类曲线积分</font>
- **第一格林恒等式**：设 $u,v$ 连续可偏导，则
- **第二格林恒等式**：
  - **证明**：详见[偏微分方程](../../偏微分方程/0.衔接.md)

### Green定理

- **格林定理**：设 $D$ 是单连通区域，$P,Q$ 连续可偏导，则下列命题等价
  - **封闭归零性**：对 $D$ 内任意分段光滑闭曲线 $L$，都有 $\int\limits_L Pdx+Qdy = 0$
  - **不定积分性**：$D$ 内任意曲线积分值都与路径无关
  - **原函数存在性**：存在通积分 $U(x,y)$
  - **恰当性**：$\dfrac{\partial P}{\partial y} = \dfrac{\partial Q}{\partial x}$
  - **证明**：
    - $1\LR 2$：类似复变函数，端点确定为 $a,b$ 时，取任意 $L_1,L_2$ 都能组成闭曲线，故两者积分值相等
    - $2\LR 3$：构造上限函数 $U(x,y) = \dis\int^{(x,y)}_{(x_0,y_0)}Pdx + Qdy$，中值定理 + 上限函数连续性，得两个偏导数相等，从而得到全微分相等
    - $3\LR 4$： 已知通积分，故有 $dU = Pdx + Qdy$，即 $P = U_x，Q = U_y$。用偏导数均连续 $\LR$ 混合偏导数相等
- **求通积分**
  - 用最简单的积分路径——正方形积分
  - 得到恰当方程的求解结果：$\varPhi(x,y) = \int^x_{x_0}P(x,y)dx + \int^y_{y_0}Q(x_0,y)dy + C$
- **非封闭曲线的Green公式**：$$\int\limits_{\overset{\frown}{AB}}Pdx+Qdy = U(x_B,y_B) - U(x_A,y_A)$$
  - **证明**：上限函数相减，与其下端点无关
  - **理解**：就是全微分形式的N-L公式
- **复周线定理**：同复变函数

### 习题

- **简化曲线积分**：若满足恰当性
  - **法1**：$L$ 端点不变时，可将其改为较简单的形状（比如分段直线 $L_x$ 和 $L_y$）
  - **法2**：直接用ODE的知识求通积分
- **求被积函数（高数题。。。）**：利用恰当性 + ODE直接求即可
  - **实例**：设 $\p$

### 常用二级结论

- 设 $L$ 是有限分段光滑闭曲线，$f$ 连续，则 $\dis\int_L f(x^2+y^2)(xdx+ydy) = 0$
  - **证明**：首先写成分段积分相加的形式，再换元 $u_i = x_i^2(t) + y_i^2(t)$ 得 $I = \dis\frac{1}{2}\sum^n_{i=1}\int^{u_i}_{u_1} f(u_i)du_i = \frac{1}{2}\int^{u_n}_{u_1} f(u)du = 0$
- **极坐标换元结论**：
  - 设 $D$ 是圆形区域 $(0,0)，r\leq a$，$L = \pa D$ 为其边界
  - 给定 $\D f = g(x,y)$，则可计算 $I = \dis\int_{\pa D} fds = \iint_D (xf_x+ydy)dxdy$
  - **证明**：
    - 取极坐标换元，则被积函数转化为 $f_x\cos\t + f_y\sin\t$
    - 再由格林公式，$\dis\int_L f_xdy-f_ydx = \iint_D \D fdxdy = G$
    - 再由极坐标换元，$\dis\int_L f_xdy-f_ydx = \int^{2\pi}_0 (f_x\cos\t + f_y\sin\t) d\t$
    - 即 $I = \dis\int^a_0 rGdr$

## 曲面积分 $\LR$ 三重积分

- **三维单连通区域**：若 $\Omega\subset \R^3$ 内的任意封闭曲面所围成的立体仍然属于 $\Omega$，则称 $\Omega$ 为单连通趋于
- **三维复连通区域**
- **Gauss公式**：设 $\Omega$ 是单连通区域，$P,Q,R$ 在其内可微 $$\iiint\limits_{\Omega}(\frac{\partial P}{\partial x} + \frac{\partial Q}{\partial y} + \frac{\partial R}{\partial z})dxdydz = \iint\limits_{\partial \Omega} Pdydz + Qdzdx + Rdxdy$$
  - **理解**
  - **证明**：累次积分（分离合并法）
    - 以 $R$ 为例，$\dis\iiint\limits_{\Omega}\frac{\partial R}{\partial z}dxdydz = \iint\limits_{\Omega_{xy}}dxdy\int^{x_2(x,y)}_{x_1(x,y)} \frac{\partial R}{\partial z} dz$ 
      - <font color='chartreuse'>利用累次积分公式，将（三重积分）分离为（垂线积分）和（平面投影积分）</font>
    - 用N-L公式化为 $\dis\iint\limits_{\Sigma} R\Big( x,y,z_1(x,y) \Big)-R\Big( x,y,z_2(x,y) \Big) dxdy$
      - <font color='chartreuse'>对垂线积分应用N-L公式。其中 $\Sigma$ 是 $\Omega$ 在 $z$ 轴两端的曲面边界</font>
    - 调整符号得 $\dis\iint\limits_{\Sigma_2}R + \iint\limits_{\Sigma_1}R = \iint\limits_{\partial\Omega}R(x,y,z)dxdy$
      - <font color='chartreuse'>跟格林公式相同的讨论，发现可以融合为一个曲面积分</font>
  - **体积计算公式**

### 习题

- **复周线法（三维版本）**：设 $r = \sqrt{ax^2+by^2+cz^2}$，$S$ 是任意不过原点的闭曲面，求 $$I = \iint_{S} \frac{x}{r^3}dydz + \frac{y}{r^3}dxdz + \frac{z}{r^3}dxdy$$

## 曲线积分 $\LR$ 曲面积分

- **曲面的边界**：空间曲线
- $\partial\Sigma$ 的诱导定向：逆时针的右手定则
- **Stokes公式**：设 $\Sigma$ 是分段光滑曲面，$P,Q,R$ 在其上有连续偏导数，则 $$\int\limits_{\partial\Sigma}Pdx+Qdy+Rdz = \iint\limits_{\Sigma}(\frac{\partial R}{\partial y} - \frac{\partial Q}{\partial z})dydz + (\frac{\partial P}{\partial z} - \frac{\partial R}{\partial x})dzdx + (\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y})dxdy$$
  - 即  $$\begin{cases} \dis\int\limits_{\partial\Sigma} P(x,y,z)dx = \iint\limits_{\Sigma}\frac{\partial P}{\partial z}dzdx - \frac{\partial P}{\partial y}dydx \\\\ \dis\int\limits_{\partial\Sigma} Q(x,y,z)dy = \iint\limits_{\Sigma}\frac{\partial Q}{\partial z}dzdy - \frac{\partial Q}{\partial x}dxdy \\\\ \dis\int\limits_{\partial\Sigma} R(x,y,z)dz = \iint\limits_{\Sigma}\frac{\partial R}{\partial x}dxdz - \frac{\partial R}{\partial y}dydz \end{cases}$$
  - **理解**：空间中的Green公式
  - **证明**：以标准曲面和 $P$ 为例，
    - 由第二类曲线积分的投影法，$\dis\int\limits_{\partial\Sigma}P(x,y,z)dx = \int\limits_{\partial \Sigma_{xy}}P(x,y,z(x,y))dx$
    - 在 $xy$ 平面上对其应用Green公式，化为二重积分即可
  - **行列式形式**：$\dis\int\limits_{\partial\Sigma}Pdx+Qdy+Rdz = \iint\limits_{\Sigma} \begin{vmatrix}\cos\alpha & \cos\beta & \cos\gamma \\ \dfrac{\partial}{\partial x} & \dfrac{\partial}{\partial y} & \dfrac{\partial}{\partial z} \\P & Q & R \end{vmatrix}dS$
    - <font color='chartreuse'>方向由右手定则确定，即右手伸出大拇指，四指旋转方向是 $\pa\Sigma$ 正方向，拇指方向是 $\Sigma$ 正方向</font>


### Stokes定理

- **Stokes定理**：设 $\Omega\subset\R^3$ 是单连通区域，若 $P,Q,R$ 在其上可连续偏导，则以下条件等价
  - **封闭归零性**：对任意分段光滑曲线 $L$，$\dis\int_L Pdx + Qdy + Rdz$ 与路径无关
  - **不定积分性**：对任意分段光滑曲线 $L$，$\dis\int_L Pdx + Qdy + Rdz$ 与路径无关
  - **势函数存在性**：存在 $u$ 使得 $du = Pdx + Qdy + Rdz$
  - **恰当性**：$P_y = Q_x，Q_z = R_y，R_x = P_z$
  - **证明**：和格林定理相同